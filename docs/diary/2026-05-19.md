# 2026-05-19 — Sezení 1

První sezení projektu. Pracovní adresář ještě `u_bonexu/` (přejmenování na `pic2omap/` po sezení).

## Diskuse

- Uživatel chtěl vektorizovat `u_bonexu.png` (raster orienťácká mapa) do OOM podle podkladu.
- Vysvětlil jsem, proč plná end-to-end auto-vektorizace není reálná: sémantika ≠ barva, pattern fills, překryvy, Bezier fitting, low-res vstup. Doporučil jsem standardní postup (raster jako template, ručně přes něj kreslit).
- Uživatel souhlasil, pivot na meta-úkol: **analýza, co by obnášela výroba nástroje `pic2omap`** (%THINK).
- Provedli jsme strukturovaný brainstorm (8-stage pipeline, klíčové problémy, 4 alternativní přístupy A–D, rizika, fázový plán).
- Před zápisem nápadů jsme provedli **hloubkovou rešerši internetu** — existuje něco podobného? Agent našel: CoVe (integrovaný v OOM), Karttapullautin (LiDAR, ne raster), NYPL map-vectorizer (Sanborn maps), akademické U-Net papery na historical maps. Žádný end-to-end raster→OMAP. OCAD **nemá** auto-vektorizaci raster scanu (opraveno dřívější chybné tvrzení).

## Rozhodnutí

- Projekt: `pic2omap` (přejmenování adresáře `u_bonexu/` → `pic2omap/` udělá uživatel po %END).
- Struktura: `resources/` (vstupní rastry), `IDEAS.md` (brainstorming), `RESEARCH.md` (externí zdroje, průběžně updateován), `DIARY.md` + `docs/diary/`.
- Strategie: stavět **nad CoVe** (color line vectorization už integrovaná v OOM, GPL-3.0). Fokus pic2omap na to, co CoVe **neumí**: area symbols, point symbols, automatické napojení na ISOM/ISSprOM (symbol recognition raster→ISOM = "díra v trhu").
- OMAP formát je veřejně dokumentován → OMAP writer napsat v Pythonu bez závislosti na OOM C++.
- Žádný kód, žádný `git init` v tomto sezení (vše až po přejmenování adresáře).

## Kód

Žádný.

## Příště

1. **Přejmenovat adresář** `u_bonexu/` → `pic2omap/` (uživatel ručně).
2. **Přesunout memory dir** `~/.claude/projects/C--Users-mrkla-source-u-bonexu/` → `~/.claude/projects/C--Users-mrkla-source-pic2omap/` (jinak se memory ztratí).
3. **`git init`** + první commit v přejmenovaném adresáři.
4. **Fáze 0 (1 den)**: definovat scope pic2omap — jeden typ vstupu (např. OOM rendered PNG ≥ 300 DPI, ISOM 2017-2), jedna metrika úspěchu. Zodpovědět otevřené otázky z `IDEAS.md`.
5. **Prozkoumat CoVe**: nainstalovat, vyzkoušet na `resources/u_bonexu.png`, posoudit kvalitu výstupu pro liniové prvky.

---

# Sezení 2

Pokračování téhož dne. Cíl: Fáze 0 → začít databází symbolů (zadání uživatele).

## Diskuse

- Adresář přejmenován na `Pic2Omap/`, memory dir přesunut — hotovo mimo sezení.
- **Strategie symbolové DB**: namísto manuálního psaní YAML z ISOM PDF (200+ symbolů, riziko překlepů) extrahovat z existujících OMAP souborů. OMAP je XML s plnou definicí symbolů uvnitř — jediný zdroj pravdy, žádná divergence vůči OOM.
- **Architektura ve 2 vrstvách**:
  - Vrstva 1 (`SymbolLibrary`) = rich 1:1 model z OMAP
  - Vrstva 2 (`ColorProfile`, později `SymbolProfile`) = derivovaný model pro detector
- **Paralelní agenti** verifikovali oba OMAP soubory proti IOF PDF spec (ISOM 2017-2 + ISSprOM 2019-2).
- **Klíčové zjištění**: oba OMAP soubory používají **ISOM 2000 / OOM-default schéma**, ne IOF 2017-2 ani ISSprOM 2019-2. Číslování symbolů je posunuté o 1-3 pozice, některé prohozené. Stejný kód = různý symbol napříč spec verzemi (501 = Road v ISOM 2000/2017-2, Paved area v ISSprOM 2019-2).
- **Color separation strategy**: palette-based (fixní paleta z library, nearest in LAB), ne K-means. Deterministické, žádný unsupervised clustering.
- **Anti-aliasing problém**: vrstevnice se rozsekly mezi Brown a OpenOrienteering Orange (LAB distance malá). Řešení: `ColorCategory` vrstva — sloučit blízké barvy do sémantických rodin (BROWN, GREEN, BLACK, YELLOW, BLUE, PURPLE, WHITE, GRAY, RED) podle HSV hue.

## Rozhodnutí

- **DB schéma**: OOM-native, ne IOF přepisování. Pic2Omap bude pracovat s kódy, jak jsou v OMAP (širší kompatibilita s existujícími mapami).
- **Jazyk**: Python (potvrzeno, stdlib + numpy + opencv + skimage).
- **Symbol library**: extrahovat z OMAP parserem, ne ručně z PDF.
- **Color separation**: palette-based, ne K-means.
- **Color category**: HSV hue klasifikace + manuální NAME_OVERRIDES pro hraniční případy (Green 50% Yellow patří k GREEN, ne YELLOW).
- **Spec PDF**: stáhnout pro offline referenci do `docs/`, ale **negitovat** (22 MB, public download). Gitignore.
- **První git commit**: po dokončení dnešního sezení (~1100 řádků kódu + reports).

## Kód

Nové Python moduly (~600 řádků kódu + komentáře):

- `omap_model.py` — dataclassy: `Color`, `SymbolBase` + 5 podtypů (Point/Line/Area/Text/Combined), `SymbolLibrary`, enums (`SymbolType`, `CapStyle`, `JoinStyle`). Jednotky OMAP = mikrometry.
- `omap_parser.py` — XML parser pomocí `xml.etree.ElementTree` (stdlib, žádné lxml). Namespace handling, type dispatch, color reference resolution.
- `color_profile.py` — vrstva 2 nad SymbolLibrary: `ColorProfile` s precomputed LAB hodnotou. `deduplicate_by_rgb()` pro odstranění knockout duplicit.
- `color_category.py` — `ColorCategory` enum + classifier (HSV hue thresholds + manuální overrides). `build_category_map_with_overrides()`.
- `color_separator.py` — Stage 2 pipeline: `separate_colors()` (palette-based, LAB distance), `merge_masks_by_category()`, `make_overview()`, `save_masks()`, `save_category_masks()`.
- `dump_symbols.py` — CLI overview symbolové DB (kolik barev/symbolů per typ, výpis ISOM kódů).
- `separate_demo.py` — CLI color separation: per-priority + per-category masky, overview, report.

Nové dokumenty v `docs/`:

- `dump_forest_sample.txt`, `dump_complete_map.txt` — dump symbolové DB pro oba OMAP soubory.
- `spec_check_ISOM-2017-2.md` (388 řádků) — verifikace forest sample proti IOF ISOM 2017-2 Rev 6 PDF. **Hlavní nález**: OMAP používá ISOM 2000 numbering, ne 2017-2.
- `spec_check_ISSprOM-2019-2.md` (246 řádků) — verifikace complete map proti IOF ISSprOM 2019-2 Rev 6 PDF. **Hlavní nález**: complete map taky používá ISOM 2000 (ne ISSprOM 2019-2), navzdory měřítku 1:4000.
- `ISOM_2017-2_rev6.pdf` (11.7 MB, gitignored), `ISSprOM_2019-2_rev6.pdf` (10 MB, gitignored), `maprunner.pdf` — lokální PDF reference.
- `omap_symbols_list.txt` — kompletní seznam OMAP code+name páry.

Výstupy testu v `output/forest sample/`:

- `priority/` — 16 binárních masek per palette barva.
- `category/` — 8 sloučených masek per ColorCategory.
- `_overview.png` — quantized verze celého obrazu (vizuálně k nerozeznání od originálu).
- `_report.txt` — statistika per barva (% pixelů).

## Stav pipeline

| Stage | Popis | Stav |
|---|---|---|
| 1 | Preprocess (deskew, denoise) | ☐ |
| **2** | **Color separation** | **✓ funguje na forest sample** |
| 3 | Per-color raster ops (morfologie, components, skeleton) | ☐ |
| 4 | Symbol recognition | ☐ |
| 5 | Vektorizace | ☐ |
| 6 | Topology fix | ☐ |
| 7 | Georeferencing | ☐ |
| 8 | OMAP XML serializace | ☐ |

## Příště

1. **Stage 3 — per-category raster ops**:
   - Morfologie: opening/closing pro odstranění šumu (1px artefakty z anti-aliasing)
   - Connected components: rozdělit kategorie na linky / plochy / body
   - Skeletonizace líniových masek → 1px středovky (přípravka pro Stage 5)
2. **Ground truth comparison**: napsat skript, který načte `forest sample.omap` a porovná: kolik vrstevnic je v OMAPu (Brown linie 101/102/103) vs kolik detekuje pipeline z PNG.
3. **Map area extraction**: title "Forest map sample" je nad mapou, zbytečně rozhazuje statistiku. Buď automatický crop (detekce mapového obdélníku), nebo manuální ROI.
4. **ISSprOM template lookup** — pokud sprint je v rozsahu, sehnat oficiální `ISSprOM_2019-2.omap` template z OOM symbol sets distribuce (https://github.com/OpenOrienteering/symbol-sets nebo OOM installdir).
5. **(Bonus) SymbolProfile** — vrstva 2 pro symboly (pro Stage 4). Per-symbol: key features (line width, dashed Y/N, point shape, area pattern).

---

# Sezení 3

Pokračování téhož dne. Cíl: **Stage 3 — per-color raster ops** (morfologie + connected components + skeletonizace).

## Diskuse

- **Strategie morfologie**: konzervativní default `opening 2×2` pro všechny barvy, žádné `closing`. Důvod: vrstevnice na renderu jsou ~1px tlusté, agresivnější kernel by je smazal. Closing skrývá problémy, neřeší — vrátíme se k němu po vizuálním zhodnocení.
- **Klasifikace komponent na POINT/LINE/AREA**: iterativní ladění heuristiky.
  - v0: `area + density` → fragmenty vrstevnic mylně klasifikovány jako POINT.
  - v1: přidána `POINT_MIN_DENSITY=0.4` → posun 55 fragmentů z POINT do LINE.
  - v2 (finál): přidán `POINT_MAX_ASPECT=2.0` (kulatost / čtvercovitost bboxu) → posun dalších 83 komponent z POINT, většina (78) do AREA, 5 do LINE. POINT teď vyžaduje **smallness AND compactness AND kulatost** — tři podmínky.
- **WHITE problém**: pozadí (open forest 401) má v rasteru rozsekané komponenty (bbox skoro celá mapa, nízká density) → mylně klasifikováno jako LINE. **Řešení**: `split_category_masks` WHITE skipuje. Pokud se v budoucnu objeví potřeba detekovat open forest jako AREA symbol, řeší se to separátně.
- **Skeleton brown tloušťka 3×**: vrstevnice na rendered PNG mají efektivně 3 pixely (po 2×2 opening). Ne chyba, jen kalibrace na DPI. Není potřeba řešit teď.
- **AREA leak**: krátké tlusté linkové fragmenty (density > 0.5) se schovaly v AREA. Stage 4 bude potřebovat AREA min-size filter, aby je odlišil od ploch.

## Rozhodnutí

- **Klasifikační prahy** (kalibrované pro forest sample 631×478, ~300 DPI render):
  - `MIN_AREA = 4` (NOISE)
  - `POINT_MAX_AREA = 30`, `POINT_MIN_DENSITY = 0.4`, `POINT_MAX_ASPECT = 2.0`
  - `AREA_MIN_DENSITY = 0.5`
  - Per-DPI škálování zatím není (TODO).
- **WHITE skip** v components/skeleton (nikoli v morfologii — morfologie WHITE prostě nezmění, params `open_kernel=0`).
- **Morfologie konzervativní**: `opening 2×2` pro všechny barevné kategorie, žádné closing.
- **OpenCV** pro morfologii a components, **scikit-image** pro skeletonize (Zhang-Suen — robustnější než morphological thinning).
- **Stage 3 MVP cíl splněn**: `cat_brown_skeleton.png` = čisté 1px středovky vrstevnic, připraveno pro Stage 5 vektorizaci.

## Kód

Nové Python moduly (~430 řádků + komentáře):

- `morphology.py` — `MorphParams` dataclass, `CATEGORY_PARAMS` dict, `clean_mask`, `clean_category_masks`, IO helpery, `format_stats_report`.
- `components.py` — `ComponentType` enum (POINT/LINE/AREA), `ComponentInfo` dataclass s `density` a `aspect_ratio` props, `classify_component`, `extract_components` (cv2 wrapper), `split_by_type` (LUT-based vectorizace), `split_category_masks` se skipem WHITE.
- `skeleton.py` — `skeletonize_mask` (bool roundtrip kolem skimage), `skeletonize_line_masks`, IO, `format_skeleton_report` (počítá poměr LINE px / Skel px jako proxy tloušťky).
- `stage3_demo.py` — CLI orchestrátor: morphology → components → skeleton, `_stage3_report.txt`.

Výstupy v `output/forest sample/`:

- `morphology/` — 8× `cat_<color>_clean.png`
- `components/` — 21× `cat_<color>_<type>.png` (7 kategorií × 3 typy, WHITE vynechána)
- `skeleton/` — 4× `cat_<color>_skeleton.png` (brown, green, black, yellow)
- `_stage3_report.txt` — všechny tři kroky v jednom textu

## Stav pipeline

| Stage | Popis | Stav |
|---|---|---|
| 1 | Preprocess (deskew, denoise) | ☐ |
| 2 | Color separation | ✓ |
| **3** | **Per-color raster ops** (morfologie, components, skeleton) | **✓ funguje na forest sample** |
| 4 | Symbol recognition | ☐ |
| 5 | Vektorizace | ☐ |
| 6 | Topology fix | ☐ |
| 7 | Georeferencing | ☐ |
| 8 | OMAP XML serializace | ☐ |

## Statistika finálního běhu (forest sample)

```
Krok 1 — Morfologie (opening 2×2):
  brown:   36480 → 32441  (-11.1%)
  green:   53964 → 52455  (-2.8%)
  yellow:  14139 → 12373  (-12.5%)
  black:   20581 → 19091  (-7.2%)
  gray/blue: téměř smazány (artefakty)

Krok 2 — Components (POINT / LINE / AREA):
  brown:   291 / 181 / 111
  green:   196 /  65 / 243
  black:   226 /  71 /  87
  yellow:   85 /  16 /  29

Krok 3 — Skeletonizace LINE:
  brown:   16366 px → 5530 px  (~3× tloušťka)
  black:   13699 px → 4182 px  (~3.3×)
  green:   31818 px → 4936 px  (~6.5×, šrafa zelené)
  yellow:   4826 px →  936 px  (~5.2×)
```

## Příště

1. **Stage 4 — Symbol recognition pro vrstevnice (Brown)**: první symbol detector. Vstup `cat_brown_skeleton.png` → rozdělit na 101 Index contour / 102 Contour / 103 Form line podle tloušťky původní čáry a stylu (form line = čárkovaná).
2. **AREA filtrování**: krátké tlusté linkové fragmenty se schovaly v AREA. Min-size threshold nebo pattern filter, aby Stage 4 dostal čisté plochy.
3. **Ground truth comparison**: `compare_to_omap.py` — naparsovat `forest sample.omap` a porovnat počet vrstevnic v OMAP vs počet LINE komponent v `cat_brown_line.png`. Metrika úspěchu pro Fázi 0.
4. **Pattern detection** (P2 z dnešního sezení): šrafa zelené (412/413) — Fourier nebo line-density. Patří k Stage 4.
5. **(Pokud čas)** SymbolProfile vrstva 2 jako příprava pro Stage 4.

---

# Sezení 4

Pokračování téhož dne. Cíl: **Ground truth comparison** — metrika úspěchu pro Fázi 0 dřív než spustím Stage 4 detektor. Bez ní bych ladil naslepo.

## Diskuse

- **Pořadí prací**: Stage 4 vrstevnicový detektor počká, dokud nebude metrika. Důvod: bez "kolik vrstevnic je v OMAPu vs kolik jich pipeline najde" nelze říct, jestli detektor funguje.
- **Architektura skriptu**: standalone `compare_to_omap.py`, ne rozšíření `omap_parser.py` (KISS — parser zůstává čistě o symbol DB, `<objects>` parsing patří k metrikě, ne k modelu). Pipeline counts přes `extract_components` z `components.py` (DRY).
- **Strategie mapování**: každý `<object symbol="N">` → `SymbolBase.id` → typ (Line/Area/Point) → barva (`color_ref` / `inner_color_ref` / `outer_color_ref`) → `ColorCategory` přes existující `category_map`.
- **Encoding fixes**: `'Δ':>+5` v f-stringu padlo (sign-flag pro string), nahrazeno `'Diff':>5`. Windows konzole je cp1250, Δ neumí — vyhnout se unicode v reportu.
- **Klíčový nález při testu**: 156 objektů ve forest sample přeskočeno jako "no-color" (inner_color="-1"). Zahrnuje **významné mapové prvky**:
  - 70× 409 Undergrowth: difficult to run (zelená šrafa)
  - 19× 407 Undergrowth: slow running
  - 28× 416 Distinct vegetation boundary
  - 16× 110 Small erosion gully (mělo být BROWN line!)
  - 6× 115 Small depression (mělo být BROWN point!)
  - Důvod: tyto symboly mají barvu definovanou až v `<pattern>` child elementech. Naše `symbol_to_color_ref` se na patterns nedívá → GT je podhodnocený. Zapsáno jako TODO, ne fixed teď (scope creep).
- **Brown line GT=80 ≠ 69 vrstevnic**: do bucketu spadly i další brown linie (Minor road, Earth bank, Erosion gully, Earth wall). Čistou metriku pro vrstevnice 101/102/103 doplníme `--symbols` flagem (TODO).

## Rozhodnutí

- **Skript je standalone**, parser čistý (DRY by si žádalo rozšířit parser, ale jednorázový use-case ne).
- **Diff počítadlo rozdělené** na text/combined/no-color — diagnostika důležitá pro nálezy typu "16 erosion gully se schovalo jinde".
- **No-color symboly se vypisují** s počtem a kódem — okamžitě vidět, co kde uvízlo.
- **GitHub publikace**: na konci sezení push do public repa.

## Výsledky (forest sample)

```
                BLACK   BROWN   GREEN   YELLOW
GT  point         0       0       9       0
    line         85      80       0       0
    area         50      16     107      35
Pipe point      226     291     196      85
    line         71     181      65      16
    area         87     111     243      29
Ratio  line   0.84x   2.26x   inf     inf
       area   1.74x   6.94x   2.27x   0.83x
```

- **BROWN line 2.26×** — oversegmentace vrstevnic (přerušení černou + anti-aliasing).
- **BLACK line 0.84×** — pipeline podsegmentuje (cesty propojené přes křižovatky).
- **YELLOW area 0.83×** — žluté plochy propojené přes sousední hranice.
- **GREEN line=65 (GT=0)** — šrafa zelené klasifikovaná jako LINE (pattern detection TODO).
- **POINT všude leak** — anti-aliasing tečky, čísla vrstevnic, fragmenty.

## Kód

- `compare_to_omap.py` (~370 řádků + komentáře) — standalone CLI nástroj.

## Stav pipeline

| Stage | Popis | Stav |
|---|---|---|
| 1 | Preprocess | ☐ |
| 2 | Color separation | ✓ |
| 3 | Per-color raster ops | ✓ |
| **0** | **Metrika úspěchu** | **✓ counts per (cat, type)** |
| 4 | Symbol recognition | ☐ ← další |
| 5 | Vektorizace | ☐ |
| 6 | Topology fix | ☐ |
| 7 | Georeferencing | ☐ |
| 8 | OMAP serializace | ☐ |

## Příště

1. **Stage 4 — Brown vrstevnicový detektor**: vstup `cat_brown_skeleton.png`, výstup rozdělení na 101/102/103 podle tloušťky původní čáry (z `cat_brown_clean.png`) a stylu (form line je čárkovaná). Cílový GT count: 69 (po `--symbols 101,102,103` filtru).
2. **No-color symbol resolution v compare_to_omap**: opravit `symbol_to_color_ref` pro pattern areas — Undergrowth, Erosion gully, Depression měly v GT figurovat. Nutné dřív, než se budeme tvářit, že máme přesnou metriku.
3. **AREA filtrování pro BROWN/GREEN** — ratio 6.94× a 2.27× ukazuje, že Stage 3 leakuje fragmenty čar do AREA. Min-size threshold po vyhodnocení distribuce velikostí.
4. **`--symbols` flag** v compare_to_omap.py — přesná metrika jen pro určené ISOM kódy.
5. **Pattern detection (Stage 4)** — šrafa zelené (412/413) přes Fourier nebo line-density. Spojené s problémem GREEN line=65 false positives.

---

# Sezení 5

Pokračování téhož dne. Cíl: **opravit podhodnocenou GT** — 156 přeskočených objektů ve forest sample (Undergrowth, Erosion gully, Depression, …). Bez správné GT bych Stage 4 detektor ladil proti křivému targetu.

## Diskuse

- **3 strukturně odlišné no-color případy v OMAP XML**:
  - **AreaSymbol s barvou v `<pattern>`** (407/409 Undergrowth): `inner_color="-1"`, ale `<pattern color="17">` jako přímý atribut nebo `<pattern type="2"><symbol>...<point_symbol inner_color="X"></symbol></pattern>` jako nested.
  - **LineSymbol s barvou v `<mid_symbol>`** (110 Small erosion gully, 416 Distinct vegetation boundary): `color="-1"`, ale `<mid_symbol><symbol><point_symbol inner_color="7"></symbol></mid_symbol>`.
  - **PointSymbol s barvou v `<element>`** (115 Small depression, 116 Pit, 118 Special land form, 419, 420, 532, 536, …): `inner_color="-1"`, ale `<element><symbol><line_symbol color="7"></symbol></element>`.
- **Architektonický návrh — kam fix umístit**: izomorfně rozšířit model (`secondary_color_ref` field na Line/Area/PointSymbol) + parser (helpery `_secondary_color_for_{line,area,point}`). Důvod: DRY — Stage 4 detektor bude potřebovat stejnou informaci ("110 je vlastně hnědá linie"). Jednotné pojmenování fieldu napříč 3 typy (KISS, ne `pattern_color_ref` / `mid_color_ref` / `element_color_ref`).
- **2-úrovňový fallback v compare_to_omap**: `symbol_to_color_ref_with_source` vrací `(color_ref, used_secondary: bool)`. Volající `symbol_to_color_ref` se zužuje na tenký wrapper, diagnostika v reportu rozlišuje "via secondary" vs "primary".
- **Vedlejší nález — phantom objects**: `objects_without_symbol: 134` jsou template geometrie uvnitř `<symbols>` definic (souřadnice patternů/elementů), ne skutečné mapové objekty. `findall(".//object")` je chytal rekurzivně. Fix: `.//objects/object` (přímé děti `<objects>` kontejneru).
- **UTF-8 v reportu**: Windows konzole default cp1250 rozsekala diakritiku na `Zpracov�no`. Stačí `sys.stdout.reconfigure(encoding="utf-8")` na startu main.

## Rozhodnutí

- **Field name `secondary_color_ref`** napříč 3 typy (Line/Area/Point) — izomorfismus pro budoucího callera, žádná per-typ idiosynkrazie.
- **Parser helpery vzájemně rekurzivní** (point_symbol → element → point_symbol → …) — OMAP strom má omezenou hloubku (~3 úrovně), žádné riziko nekonečné rekurze, žádný depth limit.
- **Pattern resolver**: pro `type=1` (line pattern) zkoušíme přímý `color` atribut na patternu, pro `type=2` (point pattern) jdeme do vnořeného `<symbol>` wrapperu. Fallback pro neznámé `type` je nested.
- **Phantom objects → 0** je důkaz správnosti XPath fixu (každý `<object>` v `<objects>` má atribut `symbol`).
- **%AUDIT:DOCS body 1+2+3 ošetřeny** (README ratiosy, TODO/DONE/DIARY sezení 5, spec_check_ISSprOM diakritika+překlad). Body 4-6 (SSOT pipeline status) odloženy — to je architektonický úklid, samostatné sezení.

## Kód

**Změny v 3 souborech (~120 řádků + komentáře):**

- `omap_model.py` — 3× nový field `secondary_color_ref: int = NO_COLOR` (LineSymbol, AreaSymbol, PointSymbol) s docstring vysvětlením.
- `omap_parser.py` — 4 nové helpery (`_color_ref_from_symbol_body`, `_color_ref_from_wrapped_symbol`, `_secondary_color_for_line`, `_secondary_color_for_area`, `_secondary_color_for_point`) + napojení do tří `_parse_*_symbol` funkcí.
- `compare_to_omap.py`:
  - `symbol_to_color_ref_with_source` — 2-úrovňový fallback s `used_secondary: bool` indikátorem.
  - `symbol_to_color_ref` — tenký wrapper (zpětná kompatibilita).
  - `GroundTruth` rozšířen o `objects_via_secondary`, `secondary_resolved_symbols`.
  - Report — nová sekce "Resolved via secondary".
  - XPath fix `.//objects/object`.
  - UTF-8 console init v main.

## Výsledky (forest sample)

```
Zpracováno objektů: 538  (před fixem: 382)
  z toho via secondary fallback: 156
Skip — bez symbolu: 0, text: 1, combined: 0, no-color: 0
```

| Kategorie / typ | GT před | GT po | Pipe | Ratio (po) |
|---|---|---|---|---|
| BLACK line | 85 | **113** | 71 | 0.63× (bylo 0.84×) |
| BLACK point | 0 | **4** | 226 | 56.50× |
| BROWN line | 80 | **96** | 181 | **1.89×** (bylo 2.26×) |
| BROWN point | 0 | **7** | 291 | 41.57× |
| GREEN area | 107 | **196** | 243 | **1.24×** (bylo 2.27×) |
| GREEN point | 9 | **21** | 196 | 9.33× |

**Resolved via secondary** (top): 70× Undergrowth difficult 409, 28× Vegetation boundary 416, 19× Undergrowth slow 407, 16× Erosion gully 110, 12× Special vegetation 418, 6× Depression 115, …

## %AUDIT:DOCS (read-only audit + 1+2+3 oprava)

8 dokumentů projetých. Nálezy klasifikované kritické / doporučené / kosmetické.

**Opraveno dnes (kritické):**
- README.md ratiosy (sezení 3 stav → sezení 5 stav) + secondary fallback popis.
- TODO/DONE/DIARY sezení 5 záznamy (součást %END).
- spec_check_ISSprOM-2019-2.md diakritika (6× překlepy: mapítko/nepouzívá/pouzít/Pravdĕpodobný/níze/broun) + překlad 4 anglických bloků do češtiny.

**Otevřeno (doporučené, samostatné sezení):**
- Pipeline status SSOT (TODO sekce "Dokumentace pro %AUDIT:DOCS") — tabulka 1..8 existuje v 5+ místech, sjednotit přes README jako kanonický.
- Stage 0 v README sedí v tabulce 1..8 na zmatené pozici, oddělit jako cross-cutting.
- Pattern detection status nekonzistence (IDEAS/README/TODO).

**Otevřeno (kosmetické):**
- IDEAS.md "Zodpovězeno (Sezení 2)" sekce neaktualizovaná po sezení 3-4.
- spec_check_ISSprOM datum-formát rozmaz (2024-02-01 vs 2026-05-19).

## Příště

1. **Stage 4 — Brown vrstevnicový detektor**: GT je teď čistá (BROWN line ratio 1.89×, target 1.00 po implementaci `--symbols 101,102,103` filtru). Detektor rozdělí `cat_brown_skeleton.png` na 101 Index / 102 Contour / 103 Form line podle tloušťky původní čáry a stylu (form line = čárkovaná).
2. **`--symbols` CLI flag** v compare_to_omap.py — přesná metrika jen pro určené ISOM kódy (priorita ke Stage 4, abychom ladili jen vrstevnice).
3. **AREA filtrování pro BROWN/GREEN** — Stage 3 leakuje fragmenty čar do AREA. BROWN area ratio 6.94×, GREEN area ratio 1.24× (po fixu lepší, ale stále má co dohánět). Min-size threshold po vyhodnocení distribuce velikostí.
4. **Pattern detection (Stage 4)** — šrafa zelené, jediný zdroj GREEN line=65 false positives.
5. **Audit doporučené body** — SSOT pipeline status (README jako kanonický, IDEAS bez status sloupce, DIARY snapshots ponechat).

---

# Sezení 6

Velký architektonický posun: split projektu na **pic2db** (raster → DB) a **db2omap** (DB → OMAP XML). DB jako strukturovaná mezivrstva = jediný zdroj pravdy detekce.

## Diskuse

- **pic2db / db2omap split** — refrejming původního 8-stage pohledu. Pic2db zahrnuje fáze A (strip overlays: texty, loga, PurplePen) + B (recognize: areas → lines → points → re-link iterativně). db2omap je čistá serializace.
- **Anotativní claiming**, ne destruktivní — každý pixel získá claim `{symbol_id, confidence}`. Reverzibilní, debug-friendly. Iterace = update claimů.
- **Persistent IDs** napříč iteracemi přes IoU bbox matching (zatím nepoužito, jen v schema).
- **erosion_gully_v1 experiment** — tři iterace (crossing-only, endpoint blob spike, pointed cap detection), všechny over-claim. GT je jen 2 × 109 ve forest sample, detektor produkoval až 17 falešných. Závěr: discrimination 102 vs 109 vyžaduje pozici-based check (sousedi 101), ne jen thickness/cap.
- **Verify against OMAP source** — user nudge: "ze zdrojáku vidíš kolik je 109". Aggregátní compare_to_omap nezachycuje render-strukturu (110 je `LineSymbol` s `line_width=0` + tečky = mimo scope line detectoru). Memory `verify-domain-claims-against-source` zapsala.
- **Orientation jako prerequisite** — user navedl: aby filter "svislé tenké" měl smysl, musíme vědět rotaci mapy. Detekce z 601.x paralelních north lines.
- **Per-OMAP-file barevná paleta** — Slovanka2016 priority 19 = Blue, ale forest sample priority 19 = Yellow. Detector musí zkoušet vícero kandidátů barev, ne hardcoded.

## Rozhodnutí

- **`docs/db_schema.md`** = kanonický zdroj pro datový model + CLI verby.
- **MapObject** (id, symbol_code, geometry_type, category, bbox, pixel_blob_id, confidence, detected_in_iter, detection_method) + **NonMapElement** (kind, bbox, metadata) + **DBSnapshot** (kontejner per iter).
- Disk layout: `output/<sample>/db/iter_N.json` + `claim_mask_iter_N.png` (16-bit grayscale, hodnota = MapObject.id).
- CLI: subcommand router `pic2db.py {detect, list, mark, diff, export}`.
- **`mark --with-ids`** — sjednoceno (vs samostatný `number` verb).
- Brown line detector v1: thin peak → 101 Contour, thick → 102 Index contour. **Mid peak záměrně přeskočen** (neznámá hypotéza, zůstává unclaimed).
- erosion_gully_v1 **odpojen** z cmd_detect, soubor držíme jako reference s helpery (crossing_signal, pointed_cap_count) pro budoucí v2.
- Area_v1 default ISOM kód per kategorie (GREEN→406, YELLOW→403). Per-priority disambiguation odložena na v2.
- Stripe filter v area_v1 **per-category opt-in**: zapnuto pro GREEN (89 pattern objektů), vypnuto pro YELLOW (jen 9 pattern, filter ublíží legitním solid 401/403).
- **orientation_v1** jako krok 0 fáze A v cmd_detect. Fallback 0.0° pokud signál nedostačující.
- DBSnapshot rozšířen o `map_orientation_deg: float | None = 0.0`.

## Kód

- **`db_model.py`** — dataclasses MapObject / NonMapElement / DBSnapshot + JSON save/load s round-trip smoke testem. `map_orientation_deg` field přidán.
- **`pic2db.py`** — argparse subcommand router. `detect` orchestruje detektory + claim mask. `list` tabulkový výpis (filtrovatelné `--symbols`). `mark` overlay + `--with-ids` popisky. `diff`/`export` stuby.
- **`brown_line_v1.py`** — thickness peak → 101/102 + claim mask. Reuse `peak_visualizer.classify_segments`. 138 objektů na forest sample.
- **`erosion_gully_v1.py`** — `crossing_signal` (adjacent + branch pixels) + `pointed_cap_count` helpery. `refine()` funkce, ale **odpojena z cmd_detect** kvůli 0/17 precision.
- **`area_v1.py`** — generic area detector parametrizovaný `ColorCategory`. Per-category `MIN_AREA_PX` (GREEN=30, YELLOW=20), per-category stripe filter. Forest sample: 59 × 406, 26 × 403.
- **`orientation_v1.py`** — per-color binary mask (BLUE / BLACK kandidáti) + HoughLinesP + length-weighted histogram angles + dominance check. Slovanka: 0.0° s dominance 2010×, forest sample: None (fallback).
- **`compare_to_omap.py`** — `--symbols` CLI flag pro per-symbol GT filtrování (commit 0ff8308).
- **`docs/db_schema.md`** — kanonická specifikace, rozšířena o `map_orientation_deg`.
- **`peak_visualizer.py`** font_scale 0.18 → 0.2 (uživatelova preference).

## Metriky (forest sample, brown_line_v1 + area_v1)

| Symbol | Pic2db | GT | Ratio |
|---|---|---|---|
| 101 Contour | 112 | 51 | 2.20× (over-segm) |
| 102 Index contour | 26 | 15 | 1.73× (+ contaminations) |
| 406 default green | 59 | 99 solid | 0.60× (post-stripe filter) |
| 403 default yellow | 26 | 26 solid | **exact match** |
| **Celkem** | **223 objektů** | — | claimed 8.3 % pixelů |

## Resources

Slovanka2016 (`.omap` + `.png` + `.pgw`) přidán jako test data pro orientation_v1. 34 × 601.1 modré magnetic north lines, scale 1:15000, declination 3.75°. Validuje že orientation_v1 detekuje rotation rasteru, ne georef rotation.

## Memory přidána

- `erosion-gully-vs-index-contour` — discrimination signals, case studies #116/137/122/119, závěr "pozice-based check nutný v2"
- `verify-domain-claims-against-source` — XML inspekce > agregáty, `line_width=0` = point pattern
- `orientation-detection` — per-color Hough, per-OMAP-file paleta, raster vs georef rotation

## Příště

1. **Per-priority area disambiguation v2** — využít `output/<sample>/priority/priorityNN_*.png` masky k rozlišení 406 vs 408 vs 410, 401 vs 403. Pro každou detekovanou area komponentu zjistit, která priority maska má největší overlap → ISOM kód podle `inner_color`.
2. **Black area detector (526 Building)** — největší zatím neclaimed area kategorie (50 × ve forest sample). Stage 3 `cat_black_area.png`. Podobné area_v1, jen jiná default barva a kód.
3. **Stage 2/3 pipeline na Slovanka** — risk: 143 Mpx → pomalé. Možnost downscale na ~3000 px max rozměr pro stage 2/3, plus orientation_v1 už funguje. Otestuje generalization detektorů na druhou mapu.
4. **109 erosion_gully v2** — pozici-based check (kolmé sample sousedů 101). Reusable nástroje (endpoint detection, parallel sampling) pro další detektory.
5. **103 Form line** — dashed brown detector v mid peaku.
6. **Stripe filter pro rotated maps** — bbox rotation transform pokud `orientation_deg != 0`.
7. **Stage 2/3 brown contamination** — urban roads ISSprOM 529/503 v `cat_brown_clean.png`. Color separator nebo per-OMAP-version paletu.

---

# Sezení 7

Pokračování téhož dne. Cíl: %AUDIT:CODE + %AUDIT:DOCS opravy, pak per-priority area disambiguation v2 + black area detector + Slovanka2016 generalizační test.

## Diskuse

- **%AUDIT:CODE** odhalil 11 nálezů: kritické (detection_method nekonzistence, pic2db.py docstring stale, _resolve_db_path lexikografický sort, 9× duplicitní UTF-8 console fix, @TODO markery v kódu, uint16 capacity claim divergence), doporučené (adresářová struktura — odloženo, end-to-end detect — designové rozhodnutí, DBSnapshot/MapObject iter duplicita — doc-only). Vše s rychlým řešením opraveno.
- **%AUDIT:DOCS** odhalil 11 nálezů: kritické (README Pipeline status stale, README metriky ze Sezení 5, Repository layout chybí 12 souborů, IDEAS.md Zodpovězeno neaktualizováno), doporučené (Pipeline status SSOT, IDEAS.md duplicitní pipeline, db_schema.md status, RESEARCH.md datum), kosmetické (spec_check_ISSprOM přežily 2 překlepy, DIARY Sezení 6 heading inkonzistence, "step 0" → "krok 0"). Vše opraveno.
- **Per-priority area disambiguation v2**: `area_v1` rozšířen o per-component priority overlap voting. Filter OOM-specific (411.X) + sémantický filter (jen 4XX vegetation pro GREEN/YELLOW) + majority threshold 0.50. **Klíčový nález**: forest sample.omap má priority 10 "Green 50% Yellow" mapovanou na 527 Settlement (OMAP design choice), což sémantický filtr správně odmítl (22 false positives → 0). Forest sample zlepšení: 4× 408 + 3× 404 (= 8 % areas s konkrétním kódem místo default).
- **Black area detector (526 Building)**: extension `area_v1` o BLACK kategorii — žádný nový soubor, jen per-category dict entries (MIN_AREA_PX=20, DEFAULT="526", ALLOWED_ISOM_PREFIX="5"). Disambiguation v2 nepomohla — všechny solid BLACK area symboly (526/527.1/528/202) sdílí priority 1 ve forest sample. Forest sample: 59 detekováno / GT 50 = 1.18× (mírná over).
- **103 Form line experiment failed**: `form_line_v1.py` s co-linear pair heuristikou nad mid peak brown skeletonu produkoval 20 detekcí vs GT 3 = 6.7× over-claim. Stejný pattern jako `erosion_gully_v1` ze Sezení 6 — sparse GT (≤ 5) v over-segmented bucketu (43 segmentů). Memory `sparse-gt-naive-detector-trap` zachytila pattern.
- **Slovanka2016 Stage 2/3 test**: 143 Mpx → downscale na 6.5 Mpx (3000 px max dimension, scale 21%). Stage 2/3/detect proběhlo. `orientation_v1` detekoval 0.00° z 34 modrých magnetic north lines (pro forest sample fallback). Klíčový bug nalezen: `cv2.imread()` na Windows nedokáže načíst priority masky s českou diakritikou v názvech (`priority01_Bílá_nad...png`) kvůli fopen encoding. Fix: `np.frombuffer + cv2.imdecode`.
- **Generalizační nálezy ze Slovanka**: 
  - **Symbol code mismatch** — `brown_line_v1` hardkóduje `"101"`, ale Slovanka má `"101.0"` (`.0` suffix konvence). Detektor není template-aware. Memory `template-aware-symbol-codes`.
  - **v2 disambiguation funguje na custom palette** — Slovanka má dedikované priorities 24/27 (Yellow přes zelenou) → 401.1/403.1 správně.
  - **YELLOW over-detection** — 403.1 detekováno 1019× vs GT 47× = 21× over. Color separation transient pixely mezi green/yellow padají do priority 27.

## Rozhodnutí

- **`detection_method` = filename detektoru** (SLAP). `brown_line_v1` (ne `brown_thickness_v1`), `area_v1` (ne `{cat}_area_v1`).
- **`cli_utils.force_utf8_console()` helper** — extrakce DRY, 9 souborů.
- **`docs/db_schema.md` status**: "návrh (Sezení 5)" → "Implementováno (Sezení 6)".
- **README = SSOT pipeline status** — IDEAS.md 8-stages tabulka redukována na historický odkaz. TODO.md položka odstraněna.
- **`--omap` flag v `pic2db detect`** = opt-in v2 disambiguation. Bez flagu zůstává v1 default-per-category chování.
- **Sémantický filtr v `build_priority_to_area_code`**: jen 4XX pro GREEN/YELLOW, 5XX pro BLACK. Bez něho OMAP design choice "Green 50% Yellow → 527 Settlement" způsobí false positives.
- **Majority threshold 0.50** pro disambiguation — kompromis mezi over-detection (bez prahu) a missed real cases (0.60 byl moc přísný, zabil 404 detekci).
- **BLACK area `MIN_AREA_PX=20`** — preferujeme recall (budovy 20-100 px ve forest sample). Mírná over-detection (~1.2×) z balvanů akceptovaná. Per-shape filter v3.
- **`form_line_v1` odpojeno** z cmd_detect (jako erosion_gully_v1). Sparse GT detector vyžaduje multi-signal nebo multi-sample. Memory pattern zachycen.
- **`load_priority_masks` přes `np.frombuffer + cv2.imdecode`** — opencv na Windows nedokáže UTF-8 paths.
- **Slovanka downscale persistován** jako `resources/Slovanka2016_small.png` (4.7 MB) — replikabilní pipeline.

## Kód

**Audit fixes (commits 358b296, ed7be0e):**
- `cli_utils.py` (nový) — `force_utf8_console()` helper.
- `brown_line_v1.py`: `DETECTION_METHOD` rename.
- `area_v1.py`: `detection_method` per-category → "area_v1", `@TODO` markery do TODO.md.
- `pic2db.py`: docstring update, `_resolve_db_path` natural sort, UTF-8 helper import.
- 9 souborů dedupováno UTF-8 console boilerplate.
- README.md kompletně refresh (Pipeline status, detector metrics, Repository layout 12 nových souborů).
- IDEAS.md "Zodpovězeno" aktualizováno, 8-stages tabulka redukována.
- `docs/db_schema.md` status, `RESEARCH.md` datum, 3× typografie ve `spec_check_ISSprOM-2019-2.md`.
- DIARY Sezení 6 heading H2→H1 + subsekce H3→H2.

**Detector work (commits 3a9cf81, ebe4b8b, d27f80e):**
- `area_v1.py` v2:
  - `build_priority_to_area_code(library, category, category_map)` — heuristiky filter OOM-specific (411.X) + sémantický filter (`ALLOWED_ISOM_PREFIX_PER_CATEGORY`).
  - `load_priority_masks(priority_dir)` — načte priority{NN}_*.png.
  - `_disambiguate_component(component_pixels, priority_masks, priority_to_code)` — per-component overlap voting + majority threshold 0.50.
  - `_is_oom_specific_code()`, `_is_semantically_compatible()` helpers.
  - `detect()` rozšířený o `priority_to_code` parameter, `CONFIDENCE_DISAMBIGUATED=0.80`.
  - BLACK kategorie: `MIN_AREA_PX_PER_CATEGORY[BLACK]=20`, `DEFAULT_SYMBOL_PER_CATEGORY[BLACK]="526"`, `ALLOWED_ISOM_PREFIX_PER_CATEGORY[BLACK]="5"`.
- `pic2db.py`: `--omap` argparse flag, library load + per-category priority maps, BLACK area volání, `_print_area_breakdown()` helper.
- `form_line_v1.py` (nový, **disconnected**) — co-linear pair heuristika nad mid peak. Drží `_segment_orientation()` (PCA) jako reference.

**Slovanka test (commit 997928f):**
- `resources/Slovanka2016_small.png` (4.7 MB downscale).
- `area_v1.load_priority_masks` UTF-8 fix.

## Metriky

**Forest sample (iter_1, `--omap forest sample.omap`):**

| Detektor | Objekty | Kódy |
|---|---|---|
| brown_line_v1 | 138 | 112× 101, 26× 102 |
| green_area_v1 | 59 | 55× 406 default, 4× 408 (disambig) |
| yellow_area_v1 | 26 | 23× 403 default, 3× 404 (disambig) |
| black_area_v1 | 59 | všechno 526 (priority AMBIGUOUS) |
| **Total** | **282** | claimed 9.5 % |

**Slovanka2016 small (iter_1, `--omap Slovanka2016.omap`):**

| Detektor | Objekty | Pic2db | GT | Ratio |
|---|---|---|---|---|
| brown_line_v1 | 1923 | 101+102 | 587 | 3.27× (over) |
| green_area_v1 | 1115 | 406 default | 1356 | 0.82× (under) |
| yellow_area_v1 | 1482 | 401.1/403.1/403 default | 1135 | 1.31× (over) |
| black_area_v1 | 448 | 526 default | 402 | 1.11× ✓ |
| **Total** | **4968** | | | |

## Memory přidána

- `feedback-questions-format` — Q1/Q2 číslování + default odpověď
- `sparse-gt-naive-detector-trap` — GT ≤ 5 + bucket 20+ = naive heuristic vždy over-claim
- `template-aware-symbol-codes` — Slovanka `.0` suffix vs forest sample, detektory nesmí hardkódovat literály

## Příště

1. **Template-aware symbol codes** — `brown_line_v1` přijme `SymbolLibrary` jako parameter, vyzvedne exact code (101 vs 101.0). High priority (blocks db2omap export).
2. **YELLOW 403.1 over-detection** — debug color separation transient pixely mezi green/yellow. Možná closing v Stage 2 nebo per-component neighbour check.
3. **109 erosion_gully v2** — pozici-based check (sousedi 101). Reusable nástroje (endpoint detection, parallel sampling) pro další detektory.
4. **Stripe filter pro rotated maps** — bbox rotation transform pokud `orientation_deg != 0`. Slovanka má 0°, ale generalizace.
5. **Stage 2/3 brown contamination** — urban roads ISSprOM 529/503 v `cat_brown_clean.png`. Slovanka má víc city detail než forest sample.
6. **Black area per-shape filter** — Building obdélníkový vs balvan kruhový. Forest sample 526 over-detection 1.18×.
