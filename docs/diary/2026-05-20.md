# 2026-05-20 — Sezení 8

Pokračování po Sezení 7. Cíl: template-aware symbol codes (blokátor db2omap exportu),
pak YELLOW 403.1 over-detection. Bonus na závěr: db2omap PoC export.

## Diskuse

- **Template-aware symbol codes** (TODO bod 1, high priority): `brown_line_v1`
  hardkódoval `"101"`/`"102"`, ale Slovanka má `"101.0"`/`"102.0"` (`.0` suffix
  konvence). Bez resolveru by `db2omap export` nenašel matching `symbol="id"`.
  Návrh = mirror area_v1 patternu: caller resolvuje exact kódy z `SymbolLibrary`
  a předá detektoru jako stringy (detektor nezná omap_model). Memory
  `template-aware-symbol-codes`.
- **YELLOW 403.1 "over-detection" je iluze.** Začalo jako "1019× detekováno / 47×
  GT = 21× over". Diagnostika přes vizualizaci priority 27 masky odhalila skutečnou
  příčinu: OMAP má **páry area symbolů s identickou RGB**:
  - 401.0 "Open land" (priority 40) ≡ 401.1 "Louka na zelené (upraveno)" (priority 24) → rgb(1.00,0.73,0.21)
  - 403.0 "Rough open land" (priority 43) ≡ 403.1 "Paseka na zelené (upraveno)" (priority 27) → rgb(1.00,0.86,0.60)

  Color separation (palette-based, nearest LAB) **identickou RGB nerozliší** —
  `deduplicate_by_rgb` jednu zahodí (přežijí priority 24/27, masky 40/43 vůbec
  nevzniknou). Disambiguace pak mapovala přeživší priority na jediný symbol s tím
  `inner_color` = `.1` varianta. Detektor tedy lepil `403.1` (vzácná OOM-custom
  "paseka", GT 47) na vše, co bylo většinou `403.0` (rough open land, GT 898).
- **Důkaz, že to není over-detection**: detekováno 1019 bledě žlutých ploch, GT
  (403.0 + 403.1) = 945 → ratio **1.08×**. Celá žlutá: 1482 / 1135 = **1.31×** =
  normální over-segmentace. "21×" vzniklo porovnáním špatně přiřazených kódů.
  Potvrzuje memory `verify-domain-claims-against-source` (nevěř agregátům).
- **`.1` symboly mají v názvu "(upraveno)"** = OOM-custom varianty; `.0` jsou
  standardní ISOM. Uživatel (mapař) potvrdil: rozlišovat `.0` vs `.1` v první
  verzi netřeba, "paseka == louka == open land" je OK výsledek v1.
- **db2omap PoC** (uživatelův dotaz "co už dovedeme"): plný export = Stage 5/7/8,
  na závěr sezení nereálný. Dohodli jsme PoC: areas → kontury, lines → kontura
  skeletonu, georef = lineární bbox-fit, symbols/colors/georef z template OMAP.

## Rozhodnutí

- **Brown line codes**: caller (`pic2db.cmd_detect`) parsuje library JEDNOU
  (sdíleno s area v2 disambiguation), resolvuje `(thin_code, thick_code)` přes
  `resolve_brown_line_codes` a předá. Bez `--omap` fallback `"101"`/`"102"`.
- **Area RGB grouping**: `build_priority_to_area_code` seskupuje symboly podle
  **resolved RGB** (ne podle priority indexu) a vybírá základní variantu
  (`_base_variant`, nejnižší ISOM kód). Tím priority 27 → `403.0` místo `403.1`.
- **Ambiguous RGB-skupina** (víc base kódů shodné barvy, např. plná žlutá =
  401/402/412/413/415, color separation NEROZLIŠÍ) → vybrat nejnižší base
  (`401.0` = open land), NE fallback na cizí default. Důvod: nejnižší base =
  nejzákladnější symbol té barvy; fallback na jinou barvu (403.0 pro plnou žlutou)
  by byl horší. Stará ochrana `len(bases)==1 → vynech` odstraněna.
- **Template-aware default**: `resolve_default_area_code` (base z
  `DEFAULT_SYMBOL_PER_CATEGORY` → suffixovaná varianta v library). Slovanka:
  GREEN=406.1, YELLOW=403.0, BLACK=526.0.
- **db2omap PoC = forest sample** (jednodušší georef: `scale="10000"`, local CRS,
  žádný offset; Slovanka má `.pgw` + deklinaci = složitější). Areas i lines jsou
  OMAP `type="1"` (path). Template injekce: regex nahrazení obsahu `<objects>`.

## Kód

**Template-aware (brown_line_v1.py + pic2db.py):**
- `resolve_brown_line_codes(library) -> (thin, thick)` — regex `^101(\.\d+)?$` /
  `^102(\.\d+)?$`, první match, fallback `DEFAULT_THIN/THICK_CODE`.
- `detect()` + `thin_code`/`thick_code` parametry (default holé kódy).
- `pic2db.cmd_detect`: library parsing přesunut nad brown_line block (sdílí s
  area), brown codes resolved + předány, `--symbols` filtr i počítadla proti
  resolved kódům.

**Area RGB-collision fix (area_v1.py + pic2db.py):**
- `_code_sort_key`, `_base_variant`, `_priority_to_rgb` helpery.
- `build_priority_to_area_code` přepsán na RGB grouping + base-variant výběr.
- `resolve_default_area_code(library, category)` — template-aware default.
- `detect()` + `default_code` parametr (None → holý `DEFAULT_SYMBOL_PER_CATEGORY`).
- `pic2db.cmd_detect`: resolve 3× default, předány do detect_area + breakdown print.

**db2omap PoC (db2omap.py nový + pic2db.py):**
- `_compute_coord_bbox`, `_make_transform` (pixel→coord lineární fit, y-flip),
  `_object_coords_xml` (findContours + approxPolyDP), `export()`.
- `pic2db.cmd_export` implementován + `--template` argument.

## Metriky

**Slovanka2016 small (iter_1, `--omap Slovanka2016.omap`) — po fixu:**

| kategorie | kód | pic2db | GT | ratio |
|---|---|---|---|---|
| YELLOW | 403.0 (dom.) | 1182 | 898 | 1.32× |
| | 401.0 | 292 | 181 | 1.61× |
| | 404.0 | 8 | 159 | 0.05× (under) |
| GREEN | 406.1 (dom.) | 766 | 726 | 1.06× ✓ |
| | 408.1 | 302 | 630 | 0.48× |
| | 407.0 | 47 | 242 | 0.19× (pattern) |
| BLACK | 526.0 | 448 | 402 | 1.11× ✓ |
| BROWN | 101.0 / 102.0 | 1923 | 587 | 3.27× |

Před fixem: 1019× `403.1` (vzácná) + 292× `401.1` + 171× `403` default.
Po fixu: dominantní kódy sedí s GT, `403.1`/`401.1` zmizely.

**Forest sample (iter_1) — beze změny počtu (282), kódy přesnější:**
- green 49×406 (GT 45) + 6×407 + 4×408; yellow 19×403 (GT 20) + 4×401 (GT 6, nově) + 3×404.
- Žádná regrese, 401 nově rozlišeno, 406 přesnější (bylo 55).

**db2omap PoC (forest sample):** 188/282 objektů zapsáno (94 degenerátních < 3 body),
0 neznámých symbolů, validní OMAP (správný namespace, 188 namespace-aware objektů).

## Příště

1. **db2omap přesná georef** — bbox-fit roztahuje (mapa nezabírá celý PNG kvůli
   title). Potřebuje DPI nebo `.pgw` affine. Plus Slovanka georef (declination).
2. **Line vektorizace path-tracing** — vrstevnice teď exportované jako kontura
   skeletonu (zdvojená smyčka). Skeleton → polyline středem (graph traversal).
3. **Black disambiguation performance** — 448 komp × 19 priority masek ×
   full-image AND na 6.5 Mpx = pomalé (desítky s). Omezit overlap na bbox komponenty.
4. **GREEN/YELLOW under-detection** (404/407/408) — color separation slévá blízké
   odstíny. 407 je navíc pattern (šrafa), patří do pattern detektoru.
5. **109 erosion_gully v2** — pozici-based check (sousedi 101). Stále otevřené.
6. **Slovanka brown over-segmentace** 3.27× — víc city detailu, černé překryvy.

---

# 2026-05-20 — Sezení 9

Pokračování po Sezení 8. Cíl: db2omap "Příště" bod 1 (přesná georef), pak bod 2
(line vektorizace path-tracing). Oba dokončeny.

## Diskuse

- **OMAP coord je paper-space, ne reálný svět.** Object coords = pozice na
  papírové mapě v 1/1000 mm; PNG je render téhož papíru. Takže pixel → OMAP coord
  je čistá podobnostní transformace (scale + translace + y-flip), **BEZ rotace**.
  Rotace (declination/grivation), UTM a `.pgw` jsou potřeba jen pro reálný svět.
  Tohle zjednodušilo celý georef problém. Memory `georef-paper-space-not-world`.
- **Bbox-fit byl principiálně chybný.** Mapoval (0..png) → bbox template OBJEKTŮ,
  ale bbox objektů ≠ rozsah obrazu (mapa má vpravo prázdno, PNG má title).
  Empirie: Slovanka full y-osa sedí s `.pgw` (0.3176 vs 0.3175 m/px), ale x ne
  (0.2866) — protože objekty nevyplní šířku. Anizotropní protažení.
- **Scale plně určen DPI renderu**: `units_per_px = 25400 / DPI`. Ověřeno 2 zdroji
  shoda na 5 cifer: pHYs 1200 DPI → 0.31750 m/px ≡ `.pgw` √(A²+D²) = 0.31750.
- **forest sample.png je kontaminovaný** — prošel Paint.NET (tEXt tag), který
  přepsal pHYs na falešných 96 DPI (skutečné ~289), `.pgw` nemá. Nevhodný jako
  georef kalibrační cíl → zůstává na bbox-fit fallbacku. Memory `png-dpi-pitfalls`.
- **1:10000 vs scale=15000 nesoulad** — titulek Slovanky říká 1:10 000, ale OMAP
  `<georeferencing scale="15000">` i `.pgw` konzistentně implikují 1:15 000.
  Pro paper-space coords (otevření v OOM) nevadí; pro reálné UTM ano. K ověření
  uživatelem (zastaralý titulek nebo export v 1:15000).
- **Line vektorizace: greedy walk nestačí.** Topologie: 1923 line objektů, 94 %
  jednoduché křivky, 6 % junction. ALE: oněch 6 % = **15 % celkové délky** linií
  (junction objekty jsou nejdelší). Naivní greedy (1. větev) prošel u obj 1864
  jen 7 bodů z 2093 px. Změna strategie na segment-trace. Memory `skeleton-median-misleads`.

## Rozhodnutí

- **Rigorózní georef přes složení afinních matic**: `pixel --[.pgw]--> UTM
  --[projectedToMap]--> OMAP coord`. Rotace grivation z `.pgw` a z projectedToMap
  se ve složení vyruší → výsledek scale+translace. Skládám matice (numpy), neřeším
  vyrušení ručně.
- **Znaménková konvence ověřena empiricky** (4 varianty θ=±grivation, flip=±):
  vítěz `MapToProj = proj_ref + scale·1e-6 · R(-grivation) · diag(1,-1) · map`.
  Test: 100 % GT coords uvnitř PNG, y vyplní výšku; 92.9 % centroidů na ne-bílém
  pixelu; export bbox ≈ GT bbox (99 %); vizuální overlay sedí na struktury mapy.
- **Fallback řetězec**: `.pgw` + georef když je world file a CRS není Local, jinak
  bbox-fit (forest sample). MapToProj invertuju numericky (vyhne se chybám znaménka).
- **Line vektorizace = segment-trace** (uživatel zvolil možnost C po revizi).
  Kostra jako graf: uzly = deg≠2 (endpoint/junction), hrany = řetězy deg-2 pixelů.
  Každá hrana = samostatný OMAP path se stejným symbolem. Neztratí délku.
  Limitace v1: L-rohy v 8-souvislé kostře sekají segment v ohybu (víc kratších
  paths, délka zachována). Bezier + re-linking = samostatné TODO body.

## Kód

**Georef (db2omap.py + pic2db.py):**
- `_parse_pgw` (6 floatů → 3x3 pixel→UTM), `_parse_georef` (scale/grivation/ref_point
  z XML; None = Local CRS → fallback), `_build_map_to_proj` (3x3 map→UTM).
- `build_georef_transform(template, pgw, pgw_width, mask_width)` — složí
  `proj_to_map @ pgw @ scale_mask`, škáluje mask px na full px (detekce na downscale).
- `export()` + `pgw_path`/`pgw_width` param, volba georef vs bbox-fit + print cesty.
- `pic2db`: `--pgw`, `--pgw-width` argumenty.

**Line vektorizace (db2omap.py):**
- `_trace_skeleton` přepsán na segment-trace: vrací `list[np.ndarray]` (segmenty).
  Graf walk mezi uzly přes deg-2 pixely, `visited` edges set, čisté smyčky zvlášť.
- `_object_coords_xml` → `_object_coords_list` (vrací `list[str]`); line → víc
  `<coords>`, area → 1 (findContours beze změny). `export()` smyčka obalí každý.

## Metriky

| sample | objektů před | objektů po | degenerátních | georef |
|---|---|---|---|---|
| Slovanka small | 3759 | **5968** | 1209 → 95 | .pgw rigorózní |
| forest sample | 188 | **451** | 94 → 25 | bbox-fit fallback |

- Skok objektů = segment-trace zachytil krátké linie (dřív degenerátní zdvojené
  smyčky < 3 body) + segmenty větvených objektů.
- Obj 1864 (slité vrstevnice): greedy 7 bodů → segment-trace 111 bodů / 32 segmentů,
  celý pokrytý.

## Příště

1. **Bezier fit** — OOM používá kubické Beziery (Schneider fit), ne polyline.
   Nahradit approxPolyDP polyline za Bezier segmenty s flagy v `<coords>`.
2. **Re-linking fragmentů** — vrstevnice silně fragmentované (median 16 px) kvůli
   mid-symbol/černým překryvům. Spojit co-linear sousední segmenty (fáze B re-link).
3. **L-roh merge** — segment-trace seká v 8-souvislých rozích (32 segmentů u 1864).
   Sloučit segmenty pokračující přímo přes deg-3 roh (straightest continuation).
4. **1:10000 vs 15000** — ověřit Slovanka scale (titulek vs georef).
5. **Black disambiguation performance** (z dřívějška) — crop overlap na bbox.
6. **GREEN/YELLOW under-detection** (404/407/408) — color separation slévá odstíny.

---

# 2026-05-20 — Sezení 10

Pokračování po Sezení 9. Cíl: db2omap "Příště" bod 3 (L-roh merge). Bonus: orientace
fix, georef parsing fix, třetí testovací pár Garching (ISSprOM sprint) end-to-end.

## Diskuse

- **L-roh merge: 89 % deg≥3 uzlů jsou staircase, ne větvení.** Probe (`lcorner_probe`)
  na Slovance: z 641 uzlů (deg≥3) je 573 staircase (dva sousedé jsou navzájem
  8-sousedé → zalomení/diagonála 8-souvislé Zhang-Suen kostry), jen 68 pravých
  junctionů. obj 1864: 17 staircase + 1 junction. Diary Sezení 9 mluvil o "deg-3
  rozích" obecně — data zpřesnila, že drtivá většina jsou staircase artefakty.
- **Pravý junction ve vrstevnici = dotyk s jiným objektem** (uživatel, mapař):
  větvená vrstevnice neexistuje; deg-3 junction = napojení na jiný hnědý (val,
  jáma) nebo černý (kámen). Proto sloučit "rovně" přes pravý junction by chybně
  spojilo dva různé prvky → ostré větvení nechat dělené. Potvrzuje merge design.
- **N↔S flip ve forest exportu** (uživatel otevřel v OOM). Validace Sezení 9
  (100 % coords uvnitř PNG, 92.9 % na ne-bílém, y vyplní výšku) je **slepá vůči
  vertikálnímu flipu** — mapa má obsah všude, překlopená projde všemi agregáty.
  Past z memory `verify-domain-claims-against-source`.
- **OMAP object y roste DOLŮ** (top-down, paper-space). Ověřeno ze zdrojáku
  Mapperu `georeferencing.cpp::updateTransformation`: `scale(scale, -scale)`
  flipuje y až na rozhraní map↔projected (kompenzace UTM north-up), ne uvnitř
  paper-space. Pixel y taky dolů → pixel→map NEMÁ mít y-flip. Slovanka rigorózní
  byla správně (skládá pravý Mapper vzorec), bbox-fit `_make_transform` měl flip
  navíc (mylný předpoklad "OMAP y nahoru"). Nekonzistence dvou georef větví.
- **Garching odhalil georef parsing bug.** `_parse_georef` bral první `<ref_point>`
  v souboru. Garching má MAP ref (10,35) před `<projected_crs>` → vzal by ho jako
  projected (514000 UTM by mělo být). Slovanka fungovala náhodou (neměla map ref).
  Plus ignorovaný map_ref subtrakce a `auxiliary_scale_factor`.
- **complete map (Garching) je sprint ISSprOM 1:4000.** Původní PNG byl poster
  (title + legenda vlevo) → bbox-fit by brutálně protáhl; navíc `.omap` obsahuje
  legendu jako objekty (nafukuje GT bbox). Uživatel dodal čistý map-only raster
  export + `.pgw`. Mapa north-up (modré severky svislé), šikmé okraje = nepravidelná
  hranice terénu (bílé rohy mimo mapu), ne rotace.

## Rozhodnutí

- **L-roh merge = post-trace `_merge_segments`** (ne during-walk). Trasování beze
  změny + samostatná merge fáze před approxPolyDP. Čistá separace, neriskuje
  rozpojení kostry. Ve sdíleném uzlu spáruje segmenty, jejichž směry "ven z uzlu"
  míří dost opačně (linie jde rovně, ohyb < 40°). Směr z posledních k=4 px (ne 1px
  šum). Pravé větvení (žádný pár pod prahem) zůstane dělené.
- **Práh ohybu 40°** (sweep 30/40/50/60°): obj 1864 drží na ~15 segmentech napříč
  40–60° → zbylé předěly jsou skutečné junctiony, ne staircase. 40° = bezpečná
  hranice, vyšší práh riskuje slití skutečného rohu vrstevnice.
- **`_make_transform` y-flip odstraněn** — `my = min_y + (py/H)·span_y` (bez flipu).
  OMAP i pixel jsou y-down. Sjednoceno s rigorózní větví na y-down konvenci.
- **`_parse_georef` přepsán** — projected ref cíleně z `<projected_crs>` bloku,
  map ref z root-level `<ref_point>` před `<projected_crs>` (default 0,0),
  `auxiliary_scale_factor` (default 1.0). `_build_map_to_proj` odečítá map_ref
  a násobí aux: `proj_ref + scale·aux·1e-6·R(-griv)·diag(1,-1)·(map − map_ref)`.
  Věrné OOM updateTransformation.

## Kód

**L-roh merge (db2omap.py):**
- `_merge_segments(segments)` — endpoint mapa uzel→konce, `out_dir` (směr ven
  z uzlu z k px), greedy párování nejrovnějších párů pod prahem, chain-walk přes
  partner linky (linked-list). Konstanty `MERGE_MAX_BEND_DEG=40`, `MERGE_DIR_PIXELS=4`.
- `_trace_skeleton` volá `_merge_segments` před approxPolyDP. Docstring update.

**Orientace + georef (db2omap.py):**
- `_make_transform` y bez flipu (komentář opraven: OMAP y-down ze zdrojáku).
- `_parse_georef` čte oba ref_pointy + aux_scale; vrací `map_ref`, `aux_scale`.
- `_build_map_to_proj` map_ref subtrakce + aux scale (offset = proj_ref − lin@map_ref).

## Metriky

**L-roh merge (invariant pixel-set zachován u 0 objektů na obou samplech):**

| | segmenty před | po | redukce | export obj |
|---|---|---|---|---|
| Slovanka | 3018 | 2471 | −18 % | 5968 → **5421** |
| forest | 332 | 236 | −29 % | 451 → **355** |

obj 1864: 32 → 15 segmentů. Degenerátní beze změny (95 / 25).

**Georef ověření (3 páry):** Slovanka rohy beze změny (žádná regrese), forest →
None (bbox-fit), Garching → proj_ref správně (696686, 5347699) UTM, map_ref (10,35),
aux 0.999925.

**Garching end-to-end (ISSprOM sprint, 2480×3508):**
- Stage 2: 39 barev / 25 RGB, nová kategorie `gray` (budovy, 836k px).
- Detect: 1774 obj (brown 128, green 892, yellow 369, black 385), orientace 0°.
- Rigorózní `.pgw` export: 5470 obj, **bbox = GT bbox přesně** (x[−95147..91676]
  y[−75871..125803]). 154 přeskočeno (symbol neznámý), 123 degenerátní.

## Příště

1. **gray (budovy) area detektor** — `gray` kategorie (Black 50-65%, 2020 ploch
   na Garchingu) nemá detektor. Pro sprint/urbánní mapy dominantní obsah. Mirror
   area_v1 BLACK extension (MIN_AREA_PX, DEFAULT_SYMBOL, prefix).
2. **ISSprOM hierarchické/combined kódy** — 526.1 = CombinedSymbol (building =
   area fill 526.1.1 + outline 526.1.2 + point 526.1.3). `resolve_default_area_code`
   pattern `^{base}(\.\d+)?$` nechytá `526.1.1` ani combined → default "526"
   neexistuje, 154 budov ztraceno. Souvisí s TODO "CombinedSymbol parts parsing".
3. **Garching legenda v `.omap`** — GT counts nafouknuté legendovými vzorky. Filtr
   prostorově odlehlých objektů, nebo export `.omap` bez legendy.
4. **Bezier fit** (z dřívějška), **re-linking fragmentů** (bod 2 line vektorizace).
5. **Black disambiguation performance** — Garching potvrdil pomalost (gray+black
   2020+1151 ploch). Crop overlap na bbox.

---

# 2026-05-20 — Sezení 11

Pokračování po Sezení 10. Plán: bod 2 (combined kódy) → bod 1 (gray detektor).
Po empirickém ověření přehozeno na bod 1 → bod 2. Závěr sezení: **strategický obrat**
(%THINK) — fokus zpět na krok 4 (rozpoznání), export zamražen.

## Diskuse

- **"154 budov ztraceno" (diář Sezení 10) je mislabeling.** Empirie: 154 objektů
  s kódem "526" má median **26 px** (132/154 pod 50 px) → drobné černé fragmenty
  (text, rohy, kousky linií), NE budovy. Skutečné budovy jsou GRAY fill (color 6
  "Black 50-65% for buildings", rgb 89,89,89) → `classify_rgb` je dává do GRAY, ne
  BLACK. Gray kategorie (836k px) neměla detektor. Past z `verify-domain-claims-against-source`.
- **Důsledek: přehodit pořadí bodů.** Resolver (bod 2) sám by zapsal 154 fragmentů
  jako budovy = regrese. Skutečné budovy detekuje až gray detektor (bod 1) → ten první.
- **Budova v ISSprOM = CombinedSymbol 526.1** (id 111), kreslená jako area path
  (`<object type="1" symbol="111">`, 273× v GT). Parts: 526.1.1 area fill (color 6) +
  526.1.2 line outline (color 2). Helper 526.1.1 má "Do not use this symbol on its own!".
  Věrný export → povýšit na combined (fill+outline), ne helper.
- **BLACK regrese odhalena při ověření resolveru.** `resolve_default_area_code` bral
  AreaSymboly bez ohledu na barvu → BLACK default na Garchingu povýšil na 526.1
  (budova je tam GRAY). Bez category filtru by se 526.1 nalepilo na černé fragmenty.
- **Strategický obrat (%THINK na závěr).** Forest data: vrstevnice 101+102 detekováno
  138× vs GT 66× (2,1× over), `geometry_type` jen area/line (žádný point detektor),
  pokryto ~9 z ~30 symbolů. Hnědé body (115 depression, 116 pit, 112/113 knoll) bez
  point detektoru padají do brown-line a vycházejí jako falešné vrstevnice (výtka
  uživatele). Sezení 8-10 stavěla kroky 5-7 (export/georef/budovy) = střecha na slabém
  základu. *First things first* → fokus na krok 4.

## Rozhodnutí

- **Pořadí bod 1 → bod 2** (gray detektor → combined resolver). Dva body = jeden
  přírůstek "budovy na sprint mapě", ale jen v tomto pořadí dává smysl.
- **Building reprezentace = combined 526.1** (varianta A, volba uživatele), ne area
  helper. `_promote_to_combined` povýší.
- **GRAY disambiguace zapnuta** (izomorfní s BLACK) — prio 6 → budova, prio 9 → canopy.
- **Category filtr v resolveru** — default kategorie musí být symbol té barvy
  (spec-verzní rozdíl: 526 je v ISOM black, v ISSprOM gray).
- **MIN_AREA_PX[GRAY]=80** — gray bucket zaplevelený anti-alias lemy; vyšší práh
  chytá jen velké budovy, ne fragmenty (poučení z 154 BLACK fragmentů).
- **Milník v1 = "lesní core"** (vrstevnice + plochy + budovy přesně). Export (krok 7)
  zamražen, count metrika (krok 8) → stálá per-symbol DB↔OMAP validace. Mapař v smyčce
  jako oponent přes review nástroj. Viz `IDEAS.md` "Milník v1".

## Kód

**GRAY detektor (area_v1.py + pic2db.py):**
- 4 per-category dicty + GRAY (MIN_AREA=80, stripe off, default "526", prefix "5").
- `pic2db.cmd_detect`: gray_priority_map + gray_default + GRAY detect blok + breakdown.

**Combined resolver (omap_parser.py + area_v1.py):**
- `_parse_combined_symbol`: parts z `<combined_symbol><part symbol="ID"/>` → `list[int]`.
- `_promote_to_combined(library, code)`: AreaSymbol → rodičovský CombinedSymbol přes parts.
- `resolve_default_area_code`: pattern `(\.\d+)?` → `(\.\d+)*` + `category_map` filtr + povýšení.
- `build_priority_to_area_code`: `_promote_to_combined` na výsledku.

## Metriky

**Garching (ISSprOM):** gray_area_v1 241× 526.1 + 49× 526.2 + 5× 519.1.2. Budovy
median 1297 px (max 25442). Export: **220× symbol 111** (combined budova), 0× helper,
154 BLACK "526" správně zahozeno. Export obj 5470 → 5743.

**Regrese check:** forest 355 obj (beze změny), green 49×406 / yellow 19×403 stejné,
gray 0 (no-op). Resolver defaults forest 406/403/526, Slovanka 406.1/403.0/526.0 — beze změny.

## Příště

1. **Review nástroj** — `mark` overlay 3 obrázky (point/line/area) s ID v centroidu +
   `compare_to_omap` per-symbol tabulka (kód|název|GT|detekováno|ratio). Mapař oponuje.
2. **Brown-line precision audit** — 138 vs 66 GT vrstevnic: kolik mislabeled bodů vs
   over-segmentace.
3. **Point detektor (hnědé body 112/113/115/116)** — PO milníku v1. Nový
   `geometry_type="point"` + odebrat body z brown-line.
4. (Zamraženo do v1 core: bezier fit, re-linking, gray canopy over-detekce, black perf.)

## Review nástroj + georef zjištění (pokrač.)

Po %THINK obratu jsme postavili review nástroj a narazili na georef zeď.

### Diskuse

- **Krok 4 review nástroj**: `mark --by-type/--scale/--with-ids` (3 obrázky
  point/line/area, čitelné ID na upscale) + `compare_to_omap --db` (per-symbol
  tabulka GT vs DB) + `--csv-dir` (3 CSV). Mapař dostane SUMA counts + GT export
  + naše detekce k oponentuře.
- **Forest stav (review_suma)**: OK=3 (406/403/526), OVER=2 (101 2.2×, 102 1.7×),
  UNDER=4 (407/408/404/401), MISSING=28 z 37 symbolů. Žádný point detektor
  (`geometry_type` jen line/area) → hnědé body 115/116/112/113 chybí.
- **GT-master matching = slepá ulička.** Uživatel chtěl GT objekty do detailu +
  "co jsme odhalili" + nepřiřazené na konec. Postaveno (parse_gt_objects centroidy,
  pixel→coord transform, nearest-centroid greedy 1:1). Výsledek: forest 127/202
  "mislabel" = šum (416→101, 507→101 = náhodné páry). Same-type filtr nepomohl.
- **Garching `.pgw` taky nesedí** (94 % mislabel). Diagnostika: NENÍ y-flip
  (můj unáhlený závěr — coord těžiště +18218/-18011 vypadalo zrcadleně, ale obě
  budovy jsou jen kolem coord y=0, transform orientace správná). Realita: `.pgw`
  je **~5mm nepřesný** (jen 15 % budov sedí do své velikosti, 75 % do 10mm). Bbox
  shoda (sezení 10) je k flipu i k ~5mm posunu slepá. `complete map.png` je
  map-only (ne poster), ale `.omap` legendu má → nafukuje GT.

### Rozhodnutí

- **Zastavit georef hon, matching odstranit.** Per-objekt matching → TODO (potřebuje
  přesný georef + legenda filtr). Review stojí na SUMA counts (spolehlivé) + GT export
  + naše detekce vedle sebe (ruční křížení přes obrázky/OOM).
- **GT (coord) a naše (raster) pozice se 1:1 neslícují** (title pruh, nevyplnění,
  přibližný georef). review_gt.csv = OMAP coord systém (hledej v OOM), review_detail.csv
  = raster systém (hledej v `mark` obrázku). Transparentně označeno v CSV docstrincích.
- **Milník v1 fokus zůstává** lesní core; matching a georef přesnost jsou separátní TODO.

### Kód

- `pic2db.py`: `_render_mark_overlay` helper (extrakce + dim_background + scale),
  `cmd_mark` `--by-type`/`--scale` loop, argparse. Zastaralý "[NOT IMPLEMENTED]" opraven.
- `compare_to_omap.py`: `format_per_symbol_table` + `_symbol_status`, `load_db`/
  `db_symbol_counts`, `write_summary_csv`, `parse_gt_objects` (+ `GTObject`,
  `_object_centroid`, `_symbol_geometry_type`), `_coord_to_loc`, `write_gt_csv`,
  `write_detected_csv`, `--db`/`--csv-dir`. Matching kód (`match_gt_to_db`,
  `write_detail_gt_csv`) postaven a zase odstraněn (zjištění → TODO).

### Příště (review větev)

1. **Oponentura vrstevnic** — mapař označí špatně detekované 101 (přes obrázky + SUMA),
   pak cílený brown-line precision fix.
2. **Georef přesnost `.pgw`** — diagnostikovat ~5mm Garching nepřesnost (rotace/scale/
   origin), opraví i export. Pak znovu zvážit per-objekt matching.
3. **Legenda filtr** — `.omap` GT bez legendových objektů (prostorově odlehlé / mimo `.pgw`).
