# 2026-05-21 — Sezení 14

První sezení po obratu na ML (sezení 12–13). `%BEGIN` audit cadence ukázal **`%AUDIT:CODE`
práh překročen 8×** (LOC: ~4200 net přibylo od posledního auditu v sezení 7, přes dva
tracky — db2omap export, point detektory, celý ML pilot — bez jediného code auditu).
Fokus sezení: hloubkový audit kódu + odsouhlasené opravy. Trénink na „mrkla" zatím
neproběhl (jen CPU sanity `best.pt`), eval (komponenta #5) tedy odložen.

## Diskuse

- **Audit přes 7 paralelních agentů** (general-purpose), každý četl celé přidělené
  soubory v jedné vrstvě (CLI/DB, parser/color, Stage 2/3, detektory, odpojené+probes,
  export/GT, ML pilot) a vracel nálezy v kategoriích KRITICKÉ/DOPORUČENÉ/KOSMETICKÉ.
  Konsolidace + ověření kritických nálezů ručně.
- **Jediný „kritický" nález vyvrácen ověřením.** Agent hlásil záměnu `priority` ↔ index
  do `colors[]` (`get_color(ref)` indexuje `colors[ref]`, ale `build_category_map` klíčuje
  `priority`). Kontrola 3 reálných `.omap`: `priority` běží `0,1,2,…` v pořadí výskytu →
  **invariant `priority == index` drží**, není to aktivní bug, jen neověřený implicitní
  předpoklad (→ TODO assert). Past z `verify-domain-claims-against-source` — agent
  nadhodnotil, protože si nebyl jistý.
- **Dominantní motiv = DRY/izomorfismus dluh** nakupený rychlým růstem: `cmd_detect`
  area blok 4× rozkopírovaný, claim-merge 6×, `area_v1`↔`point_v1` „mirror" deklarovaný
  ale rozbitý, `_tag`/`OMAP_NS`/coord parsing/XPath duplikované 3×, georef funkce
  importované z „zamraženého" db2omap třemi moduly.
- **D2 (sdílená kostra `area↔point`) vědomě vynechána — KISS.** `area_v1.detect`
  (disambiguation + stripe + density, kód-určení provázané s rozhodováním) je řádově
  složitější než triviální `point_v1` (size+aspect). Společná `_run_component_detector`
  s `accept_fn`/`build_fn` callbacky by do čitelného kódu vnesla nepřímost kvůli ~15
  řádkům boilerplate a riskovala regresi v disambiguaci. Dvě jasné implementace > jedna
  abstraktní. Sdílené čtení masky stejně řeší `imread_unicode`.
- **Smazat vs přesunout odpojené detektory** (uživatel zvolil): `erosion_gully_v1` +
  `form_line_v1` SMAZÁNY (0 importů, helpery nikdo nevolá, v2 dle TODO vyžaduje jiný
  přístup — hodnota žije v memory + git historii). 4 nástroje (`thickness_probe`,
  `border_probe`, `border_overlay`, `point_template_poc`) ponechány v rootu — přesun do
  podadresáře by rozbil root importy (nutné `-m`), strukturní přínos malý vs práce/riziko;
  `point_template_poc` navíc brzy poputuje do produkce (TODO generalizace).

## Rozhodnutí

- **Baseline-driven regrese.** Před zásahem do detekčního toku zachycen baseline detect
  (forest sample iter_1: **722 objektů**, unclaimed 88.9 %, kompletní rozpis kódů). Každá
  dávka měnící produkci ověřena proti němu — DRY refaktor je behavior-preserving.
- **`georef.py` extrakce** (D5): `_parse_pgw/_parse_georef/_build_map_to_proj/`
  `_compute_coord_bbox/_make_transform/build_georef_transform` z db2omap → samostatný
  modul. db2omap, omap_mask i compare_to_omap importují odtud — jméno modulu už nelže
  o roli „zamražený PoC". Čistý přesun (sdílení bylo už předtím importem, ne kopií).
- **`imread_unicode` v `cli_utils`** (D3): jeden UTF-8-safe loader (`read_bytes`+`imdecode`),
  použit v 6 místech místo `cv2.imread(str(...))` (selhal by na cestách s diakritikou) i
  místo zkopírovaného `imdecode` patternu. Lazy import cv2/numpy (cli_utils zůstává lehký).
- **`omap_tag` + `iter_map_objects` kanonizace** (D4): `_tag` → veřejné `omap_tag`
  v omap_parser; phantom-fix XPath `.//objects/object` (vyloučení template geometrie
  z `<symbols>`) jako jediná funkce `iter_map_objects`. Smazány 3 lokální kopie `_tag`/
  `OMAP_NS` + 3 duplikáty XPath.
- **`_merge_claim` helper** (D1): conflict-resolution politika (dřívější claim drží pixel)
  na jednom místě místo 6 inline kopií. Brown line sjednocen (guard je tam no-op, je první).
- **point `default_code`** (izomorfismus): nová `resolve_default_point_code` (mirror
  `resolve_default_area_code`), předána z `cmd_detect`. Body teď dostanou template-aware
  kód jako plochy (forest beze změny — nemá suffixy; Slovanka by dostala „115.0" apod.).
- **ML seed** (`train.py`): `--seed` (default 42) + `set_seed` (random/numpy/torch/cuda)
  + DataLoader generator → reprodukovatelný trénink (init/aug/shuffle).

## Kód

- **Nový `georef.py`** (~270 ř.) — přesun georef funkcí z db2omap (viz Rozhodnutí D5).
- **`cli_utils.py`** — `imread_unicode(path, flags)`.
- **`omap_parser.py`** — `_tag` → `omap_tag` (31 volání), nová `iter_map_objects(root)`.
- **`pic2db.py`** — `cmd_detect` area sekce: 8 proměnných + 4 kopie bloků → `area_config`
  tabulka + smyčka; `_merge_claim` helper; point loop předává resolved default; docstring
  aktualizován (export už ne „NOT IMPLEMENTED"); erosion komentář zkrácen (po smazání souboru).
- **`point_v1.py`** — `resolve_default_point_code` + import `SymbolLibrary`.
- **`db2omap.py`** — georef funkce odstraněny, import z `georef`; docstring (rigorózní georef).
- **`omap_mask.py` / `compare_to_omap.py`** — import georef + `omap_tag`/`iter_map_objects`
  z kanonického zdroje, smazány lokální duplikáty.
- **6× `imread_unicode`** (area_v1, point_v1, brown_line_v1, peak_visualizer, point_template_poc).
- **Dávka A kosmetika** — nepoužité `import sys` (3×), f-string bez interpolace, nadbytečné
  závorky, tempfile leak ve smoke testu, `_CLASS_NAME` → veřejné `CLASS_NAMES`, dvojí
  `val_frac` default → konstanty, docstringy (db2omap/build_dataset/morphology).
- **Smazáno**: `erosion_gully_v1.py`, `form_line_v1.py`.
- Net: 22 souborů, **−609 řádků** (+ georef.py).

## Metriky / ověření

| Tok | Test | Výsledek |
|---|---|---|
| detect (forest) | 722 invariant po každé dávce A–D | ✓ identické (counts, kódy, print) |
| export (forest) | bbox-fit georef přes georef.py | ✓ 435 obj, validní bbox |
| omap_mask (Garching) | rigorózní `.pgw` georef | ✓ brown 12.9 / green 12.6 / yellow 6.5 % (= baseline) |
| compare `--db` (forest) | GT build přes `iter_map_objects` | ✓ OK=3 OVER=5 UNDER=4 MISSING=25 |
| compile všech root `.py` | py_compile | ✓ |
| db_model smoke | tempfile cleanup | ✓ |

## Kudos / Censure

- **Kudos! (uživatel):** rozhodnutí smazat mrtvé experimenty místo „přesunout jako
  referenci" (KISS) a nechat 4 nástroje v rootu — vyhnul se importnímu peklu za malý
  strukturní přínos. Věcné odpovědi bez váhání.
- **Censure! (AI, sobě):** dávka B původně schválená včetně D2 (sdílená kostra) — měl jsem
  rozsah D2 posoudit a zproblematizovat **před** žádostí o schválení dávek, ne až při
  provádění. Schvalování by mělo dostat realistický scope, ne optimistický.
- **Kudos! (AI proces):** 7-agentní fan-out audit + ruční ověření jediného kritického
  nálezu (vyvrácen) — paralelizace četby bez ztráty úsudku nad závažností.

---

## Pokračování — train tuning, %BEEP, komponenta #5 eval

Po commitu auditu pokračovalo sezení k ML pilotu: ladění tréninku, ověřovací běh
a dostavba komponenty #5 (eval) s cross-domain go/no-go.

### Diskuse

- **Časový odhad tréninku tady (CPU).** Empiricky změřeno: compute ~4 min/epocha
  (8 s/batch × 30 batchů @ batch 8; batch nemá na CPU vliv). Reálný wall-clock ~6–7
  min/epocha (compute + data-loading + val). 40 epoch ≈ 4–4,7 h; s `--workers` ~2,5–3 h;
  GPU „mrkla" ~10–30 min. Závěr: tady to JDE (podvečer), GPU je o efektivitu.
- **torch už používá 6 threadů = fyzická jádra** (12 logických = HT). Vynucení 12 přes
  HT může compute-bound trénink spíš zpomalit → `--threads` volitelný, default torch auto.
  Hlavní zrychlení dal `--workers 4` (data-loading se schoval za compute: ~4 min/epocha).
- **Cross-domain eval je trojnásobně zavádějící** (potvrzeno overlayem). Garching test
  IoU 0,122 vs within-domain 0,666 — ale (1) GT maska Garchingu je chudá (combined budovy
  526.1 chybí, `omap_mask` je vynechá) → model trestán za ROZUMNOU detekci budov (overlay:
  predikuje černé bloky na budovách, GT je nemá); (2) domain+typ gap les→sprint; (3) 15-epoch,
  1 lesní mapa. Diár sezení 12 to předpověděl. Domain gap render→sken se v pilotu nevaliduje.

### Rozhodnutí

- **`%BEEP` globální marker (hook-based, deterministický).** Volba uživatele: hook >
  CLAUDE.md instrukce (spolehlivost). `UserPromptSubmit` hook zapíše per-session flag při
  `%BEEP` v promptu, `Stop` hook na konci pípne `[console]::Beep(1200,600)` a flag smaže.
  Mimo repo (`~/.claude/hooks/` + `settings.json` + zmínka v globálním `CLAUDE.md`). Otestováno
  živě (flag diagnostika přes TEMP), beep výrazný. Per-session flag → paralelní sezení čistá.
- **Ověřovací běh 15 epoch** (ne plný 40) — ověřit `--seed`/`--workers`/tempo + jestli brown
  vzácná třída chytá s víc epochami. best.pt = 15-epoch (přepsal 5-epoch sanity).
- **Komponenta #5 = `eval.py` přes reuse `train.py`** (SegDataset/build_model/build_eval_aug/
  evaluate) — DRY, eval je jen jiný vstup (checkpoint) + vizualizace. Žádná duplikace eval loopu.
- **Go/no-go: U-Net SE UČÍ** (within-domain 0,666, dominantní třídy 0,91–0,95) = ano na hlavní
  otázku. Cross-domain blokují DATA, ne metoda → scale track (víc map, combined budovy v masce).

### Kód

- **`train.py`** — `--threads` (volitelný `torch.set_num_threads`), `--workers` help. Commit `619570c`.
- **`eval.py` (nový, komponenta #5)** — load checkpoint → `evaluate` (per-class IoU) na splitu +
  `colorize`/triptych overlay (foto | GT | predikce). CLI `--split test/val`, `--n`, `--checkpoint`.
- Mimo repo: `~/.claude/hooks/{mark-beep,beep-if-marked}.ps1` + `settings.json` hooks + `CLAUDE.md` %BEEP.

### Metriky

**15-epoch ověřovací běh** (workers 4, seed 42, ~60 min): best val mIoU **0,666** (e11) vs
5-epoch sanity 0,608. brown 0,00 → 0,25 (e12) → 0,15 — vzácná třída chytá až po ~11 epochách.

**Eval (best.pt 15-epoch):**

| split | mean IoU | bg | green | yellow | blue | black | brown |
|---|---|---|---|---|---|---|---|
| val (Slovanka, within) | **0,666** | 0,95 | 0,91 | 0,94 | 0,45 | 0,74 | 0,00 |
| test (Garching, cross) | 0,122 | 0,45 | 0,16 | 0,12 | — | 0,00 | 0,00 |

eval.py reprodukoval train 0,666 na puntík (evaluate reuse správný).

### Kudos / Censure

- **Kudos! (uživatel):** `%BEEP` nápad (vědomý marker > „pípej vždy") + rozhodné volby
  (hook, 15-epoch ověření, eval). Pěkný produktivní oblouk přes celé sezení.
- **Censure! (AI, sobě):** první „test %BEEP" selhal naslepo (config nepřenačtený) — měl jsem
  rovnou nabídnout flag-diagnostiku přes TEMP místo „slyšel jsi?". Druhý pokus už diagnostikoval.

## Příště

1. **Combined budovy v masce (526.1)** — POVÝŠENO na prioritu: bez nich je každý sprint/urbánní
   cross-domain eval znehodnocený (model trestán za detekci budov, které GT nemá). Rozbalit
   combined parts (area fill) v `omap_mask`.
2. **Plný trénink na „mrkla"** (GPU, 40 epoch) — reprodukovatelné (`--seed`), `--workers`/`--threads`
   připravené. Víc epoch dotáhne brown. Pak re-eval přes `eval.py`.
3. **Víc trénovacích map různých domén** — cross-domain blokují data; scale track (víc `.omap`
   nebo synthetic generator).
4. **Audit follow-up:** `parse_coords` kanonizace, `priority==index` assert, `compare_to_omap`
   rozdělení. Per-DPI thresholdy (D6) = samostatné sezení.
5. **`%AUDIT:DOCS`** — cadence práh blízko (6/10), výrazná změna konceptů obratem na ML.

---

# 2026-05-21 — Sezení 15

Fokus zvolen v `%BEGIN`: **combined area fill v masce (526.1)** — `[!]` priorita z minula.
Dobře ohraničené zadání, které ale odhalilo dva širší nálezy: combined fill není jen
o budovách, a hlavně — předexistující georef posun Garching masky, který zkresloval
celý cross-domain eval. Pozn.: projekt nemá projektový `CLAUDE.md`/`docs/PROMPTS.md`,
`%BEGIN` proběhl podle obecného popisu z globálních pravidel (nález pro budoucí `%CALIBRATE`).

## Diskuse

- **Premisa ověřena empiricky před kódem.** Garching `.omap`: budovy 526.1 jsou
  `COMBINED` (type 16) → objekt na ně odkazuje, ale `build_area_mask` stavěl lookup jen
  ze `SymbolType.AREA` → `sym is None` → tichý skip. Potvrzeno a **rozšířeno**: 8 z 9
  combined symbolů má AREA part (voda 304/305, marsh 309, budova 526.1, canopy 526.2,
  paved 529). 526.1 (272 obj) + 526.2 (130 obj) = 402 z 404 vynechaných.
- **Past v očekávání odhalena dopředu.** Diár sezení 14 psal „model halucinuje černé bloky
  na budovách". Data: budova 526.1 → **GRAY** (class 4), ne BLACK. Model trénoval na Slovance
  (ISOM, budovy `526` = BLACK). Tedy combined fix udělá GT kompletní, ale gray/black IoU
  zůstanou nízké kvůli ISOM↔ISSprOM class-mismatch — čestně sděleno, že mIoU nevyskočí.
- **Georef posun ≫ class-mismatch (shift-experiment).** Re-eval po opravě masky dal mIoU
  0,122 → 0,106 (poctivější, GT teď kompletní). Diagnostika overlayů ukázala, že horní pás
  fota nemá masku a spodní pás masky nemá foto → **čistý translation +129/+417 px** (bbox
  rozměry obsahu sedí, jen origin posunutý). Slovanka val funguje (green 0,91) → posun je
  **specifický pro Garching `.pgw`**. Shift masky o (−129,−417) → re-eval: mIoU **0,283**,
  green **0,617** (5×), yellow **0,351** (3×), bg **0,732**. Cross-domain generalizace ploch
  byla podhodnocená rozbitým georefem, ne metodou; zbylé nuly (gray/black/brown) = reálný
  class/data gap, georef je neopraví.

## Rozhodnutí

- **Přístup B (sjednocení), zvoleno uživatelem.** `_symbol_class(sym) -> int|None` nahrazeno
  za **`_resolve_area_fill(sym, library, sid_to_sym) -> (class, priority)|None`** — vrací
  class **i** prioritu z jednoho zdroje (dřív roztržené: class v helperu, priorita inline
  v `build_area_mask` přes `get_color(inner_color_ref)`, což by na `CombinedSymbol` spadlo —
  ten atribut nemá). Pro combined rekurzivně (1 úroveň) vezme první AREA part s výplní.
  `sid_to_sym` = všechny symboly (combined dohledává party). Zachována logika `inner=-1 → skip`
  (marsh 309, pattern overlaye) i priority-vrstvení.
- **Scope: rychlý shift-experiment, ne root-cause oprava georefu** (volba uživatele). Kvantifikuje
  podíl georefu na cross-domain mizérii bez otevření georef refaktoru (dotýká se db2omap exportu).
  Georef root-cause → povýšený TODO na samostatné sezení s baseline regresí.

## Kód

- **`omap_mask.py`** — `_symbol_class` → `_resolve_area_fill` (combined area fill + class/priorita
  z jednoho zdroje); `sid_to_sym` všechny typy; `build_area_mask` loop volá nový helper;
  importy `CombinedSymbol`/`SymbolBase`, odstraněn nepoužitý `NO_COLOR` (konzistentní se sezením 14).

## Metriky / ověření

| Tok | Test | Výsledek |
|---|---|---|
| omap_mask (Garching) | combined fill | drawn 1229 → **1538** (+309), gray **8,6 %** (budovy+canopy) |
| regrese (Slovanka) | combined bez area part | maska **bit-identická** (503.0 → skip) |
| regrese (forest) | combined-area symboly 0 objektů | maska **bit-identická** |
| georef diagnostika | raster ink bbox vs maska nonbg bbox | translation **+129/+417 px** (výška obsahu 2292 = 2292 identická) |
| eval test (původní maska) | per-class IoU | mIoU 0,106 (bg 0,40 green 0,12 yellow 0,11) |
| eval test (zarovnaná maska) | shift −129/−417 | mIoU **0,283** (bg 0,73 green **0,62** yellow 0,35) |

## Kudos / Censure

- **Kudos! (uživatel):** volba „rychlý shift-experiment" místo zabalení nebo root-cause — levné,
  vysoká informační hodnota, odhalilo skutečného viníka cross-domain bez otevření georef refaktoru.
- **Kudos! (AI proces):** ověření premisy + class-mismatch pasti **před** kódem (data, ne domněnka);
  diagnostika prázdné GT panely → systematický bbox test → kvantifikovaný posun místo „georef asi sedí".
- **Censure! (AI, dřívější sezení):** sezení 12 reality-check prohlásil Garching „alignment OK" —
  ale jen vizuálně na downscale náhledu, který 417px posun (12 % výšky) skryl. Pokrytí % (sezení 14)
  posun taky nezachytí. Reality-check potřebuje bbox/centroid kontrolu, ne jen oko na zmenšeném overlayi.

## Příště

1. **Georef přesnost Garching `.pgw`** — POVÝŠENO: ~čistý translation +129/+417 px (ne rotace, jak
   TODO tušil → snazší). Diagnostikovat root-cause (`.pgw` parsing? `georef.py` refaktor sezení 14?
   origin/ref_point?), opravit, re-eval. Dotýká se i db2omap exportu (společný `georef.py`) — baseline regrese.
2. **Plný trénink na „mrkla"** (GPU, 40 epoch) — reprodukovatelné (`--seed`), `--workers`/`--threads`.
   Po opravě georefu re-eval s poctivou cross-domain metrikou.
3. **Víc trénovacích map různých domén** — gray/black/brown blokuje class/data gap (ISOM↔ISSprOM),
   ne georef; scale track (víc `.omap` nebo synthetic generator).
4. **Audit follow-up:** `parse_coords` kanonizace, `priority==index` assert, `compare_to_omap` rozdělení.
5. **`%AUDIT:DOCS`** (7/10) + **`%CALIBRATE`** (15/15 práh + chybí projektový `CLAUDE.md`/`PROMPTS.md`).

---

# 2026-05-21 — Sezení 16

Třetí sezení dne. Fokus zvolen v `%BEGIN`: **`%CALIBRATE`** — práh 15/15 překročen
(poprvé) a sezení 15 flaglo chybějící projektový overlay. Sezení se rozrostlo o
`%AUDIT:DOCS` (uživatel: „dokonči všechny tři" zbylé nálezy) a nakonec o **opravu
root-cause georef posunu Garchingu** — `[!]` priority TODO z minula. Pozn.: od tohoto
sezení projekt **má** projektový `CLAUDE.md` + `docs/PROMPTS.md` (A1 níže), `%BEGIN`/`%END`
už nejedou jen na obecných globálních pravidlech.

## Diskuse

- **`%CALIBRATE` (poprvé, práh 15/15).** Meta-audit přes globální + (chybějící) projektový
  `CLAUDE.md`/`PROMPTS.md`, 15 diáře, permission soubory, TODO/IDEAS. Hlavní nález: **absence
  projektového overlay** — `%BEGIN`/`%END` se rekonstruovaly ad-hoc a projektové zásady
  (verify-against-source, baseline-regrese, ověření premisy, mapař-oponent) žily roztroušeně
  v memory, ne jako pracovní pravidla.
- **Role discipline OK** — README drží projektové info, DIARY changelog, globální `CLAUDE.md`
  čistý overlay. Problém byl chybějící overlay, ne jeho znečištění.
- **`%AUDIT:DOCS`** (cadence 8/10 + obrat na ML): hlavní nález `db_schema.md` „Vztah ke
  stávajícím skriptům" stale (tvrdil `pic2db detect` ještě není produkční + `detectors/`
  podadresář, co nevznikl). Spec_check + RESEARCH aktuální, bez překlepů.
- **Georef root-cause = jednotkový bug `map_ref`.** Uživatel ukázal 3 overlaye („nelícuje,
  posunuté"). Měření raster ink bbox vs maska nonbg bbox: čistý translation +142/+413 px
  (výška obsahu 2292=2292 identická). `<georeferencing><ref_point x="10" y="35"/>` je v **mm**,
  ale `_build_map_to_proj` ho odečítal od object coords v **1/1000 mm** → chybělo ×1000. Důkaz
  na dvou osách: 35 mm → 413 px, 10 mm → 118 px (přesně měřený posun). Slovanka `map_ref=(0,0)`
  (proto imunní), forest bbox-fit (`_build_map_to_proj` se nevolá) → **jen Garching trpěl** =
  vysvětluje „specifický pro `.pgw`".

## Rozhodnutí

- **A1 — projektový overlay založen** (volba uživatele, obsah odsouhlasen): `docs/PROMPTS.md`
  (`%BEGIN`/`%END` projektově — co číst, jak počítat cadence, identita sezení) + lehký projektový
  `CLAUDE.md` (doménové zásady, `%THINK` rozšíření, key-files). Vědomě BEZ project info
  (status/milníky/metriky = README).
- **A2 — permission úklid:** `.claude/settings.local.json` 34 → 5 řádků (3 venv-python wildcardy
  + pip + WebSearch), mrtvé Mapper.exe/echo/rm render testy pryč.
- **B1 — TODO/DONE prune:** 7 hotových `[x]` smazáno z TODO (všechny už v DONE = duplikáty).
- **Cadence prahy ponechány** — `%AUDIT:CODE` ≥8 sez fungoval; `≥500 LOC` early-trigger se jen
  aktivně nepočítal (teď v `PROMPTS.md` `%BEGIN` bod 2). Snižovat by zvýšilo režii.
- **Georef fix = `map_ref × 1000`** v `_build_map_to_proj` (volba uživatele: fix + regrese +
  re-eval). Baseline regrese: Slovanka/forest beze změny, Garching opraven.

## Kód

- **Nový `docs/PROMPTS.md`** (%BEGIN/%END projektově) + **nový projektový `CLAUDE.md`** (overlay).
- **`georef.py`** — `_build_map_to_proj`: `map_ref *= 1000.0` (mm → 1/1000 mm) + komentář.
- **`.claude/settings.local.json`** — konsolidace na 5 wildcardů.
- **`docs/db_schema.md`** — „Vztah ke stávajícím skriptům" přepsán (produkční realita); `category`
  komentář +gray/white/red; `detection_method` reálné názvy (`brown_line_v1`/`area_v1`).
- **`DIARY.md`** — chronologie Sezení 12 ↔ 13.
- **`TODO.md`** — prune 7× `[x]`.

## Metriky / ověření

| Tok | Test | Výsledek |
|---|---|---|
| Slovanka maska | regrese (md5 před/po fixu) | **bit-identická** (88e74aa…) |
| forest | bbox-fit (Local CRS) | imunní (`_build_map_to_proj` se nevolá) |
| Garching maska | nonbg bbox vs raster ink | posun +142/+413 → **+24/+1 px** (zarovnáno) |
| Garching overlay | vizuální | maska sedí na raster ✓ |
| eval test (Garching) | per-class IoU, opravená maska | mIoU **0,340** (bg 0,75 green **0,875** yellow 0,42) |

Cross-domain skok mIoU 0,106 → **0,340**, green 0,12 → **0,875** — lepší než umělý shift sezení
15 (0,283), protože georef oprava je přesnější. gray/black/brown 0,00 = reálný ISOM↔ISSprOM
class gap (georef neopraví).

## Kudos / Censure

- **Kudos! (uživatel):** přinesl overlaye s konkrétním pozorováním („posunuté") — odemklo
  root-cause hon, který 4 sezení vázl na „georef asi sedí". Rozhodné volby napříč sezením.
- **Kudos! (AI proces):** root-cause přes kvantitativní měření (bbox → metry → jednotková analýza
  → ×1000 ruší posun na dvou osách), ne hádání. Verify-against-source dotažený.
- **Censure! (AI, dřívější sezení):** jednotkový bug `map_ref` přežil od sezení 10 (vznik
  `_build_map_to_proj`) přes 6 sezení — bbox=GT „přesně" (sezení 10) i pokrytí % (sezení 14) byly
  k 140m translation slepé. Reality-check georefu musí být bbox/centroid test od začátku.

## Příště

1. **Plný trénink na „mrkla"** (GPU, 40 epoch) — teď s opraveným georefem = poctivá cross-domain
   metrika od začátku. Reprodukovatelné (`--seed`), `--workers`/`--threads`.
2. **Víc trénovacích map různých domén** — gray/black/brown blokuje class/data gap (ISOM↔ISSprOM),
   ne georef; scale track (víc `.omap` nebo synthetic generator).
3. **Audit follow-up kód:** `parse_coords` kanonizace, `priority==index` assert, `compare_to_omap`
   rozdělení. Per-DPI thresholdy (D6) = samostatné sezení.
4. **Volitelná README kosmetika** — sezení-značky v nadpisech, ML soubory v Repository layout.

---

# 2026-05-21 — Sezení 17

Čtvrté sezení dne. `%BEGIN` audit cadence: nic překročeno (CODE 3/8, DOCS 1/10,
prune 1/12, CALIBRATE 1/15 — tři audity proběhly v S14/S16). Stale Příště: trojice
(plný trénink / víc map / audit follow-up) se opakuje 3× po sobě, práh 5 — hlídat.
Necommitnutý kontext: po docs commitu S16 přibyly 3 commity bez diáře (imread_unicode
pro vstupní rastry + 3 nové test páry Blatná/Bedřichovka/SampleMap). Fokus zvolen
v `%BEGIN`: **scale track — validace nových párů, pak rozšíření datasetu** (Příště #2).

## Diskuse

- **3 nové páry, 2 nové domény.** `.pgw` analýza odhalila: všechny tři mají **rotovaný
  `.pgw`** (skew členy non-zero — Garching/Slovanka byly axis-aligned). Blatná ≈11.9°,
  Bedřichovka ≈10.9°, SampleMap ≈17°. Blatná + Bedřichovka mají **negativní origin →
  S-JTSK Křovák** (český národní systém, EPSG:5514), SampleMap UTM z10 (Washington, Ski-O).
  Grivation v `<georeferencing>` (11.9/10.88/17) **odpovídá `.pgw` rotaci** → dobré znamení.
- **Riziko: znaménková konvence pro Křovák.** `_build_map_to_proj` dělá `R(-grivation)@diag(1,-1)`,
  kalibrované jen na UTM (Slovanka). Křovák má jinak orientované osy → teoretické riziko flip/rotace.
  **Vhled:** projected CRS se ve složení `inv(m2p)@pgw` **zkrátí** — orientace os Křováku nevadí,
  dokud jsou `.pgw` a `m2p` konzistentní. Empirický test to potvrdil.
- **Reality-check = bbox+centroid+overlay od začátku** (poučení z S16 censure: vizuál na
  zmenšeném overlayi skryl Garching 413px posun). Diagnostický `output/scale_check.py`.
- **SampleMap 944/952 objektů skip bez třídy — NENÍ bug.** Ověřeno proti zdroji: mapa je
  převážně liniová ISOM (606 linií + 328 bodů + jen 15 area objektů, z toho 8 kreslitelných).
  `_resolve_area_fill` funguje správně; 34 AreaSymbol *typů* je definováno se zdravou klasifikací,
  jen málo *instancí*. Bílý běžitelný les = pozadí (class 0), ne plocha. → slabý dataset materiál.

## Rozhodnutí

- **Georef pipeline beze změny** — generalizuje na S-JTSK Křovák + rotovaný `.pgw` bez jediné
  úpravy kódu. Protiklad ke Garching map_ref bugu (S16): tady žádný nový režim není rozbitý.
- **Bedřichovka + Blatná do `train`** (volba uživatele), Slovanka spatial (val signál), Garching
  test, SampleMap vynechána (chudá na plochy). Celé mapy do train — leakage hrozí jen train↔val
  TÉŽE mapy, jiné mapy v train jsou čisté.
- **ASCII `name` v `DATASET_MAPS`** (`Bedrichovka`/`Blatna`) — dataset identifikátor má být anglicky
  (konvence), a stem dlaždice jde přes `cv2.imwrite(str(...))`, který selže na diakritice. Cesty
  k `resources/` diakritiku mít smí (čte je `imread_unicode`/`ET.parse` — čistý Python ji v cestě
  zvládá, na rozdíl od cv2). Robustní `imwrite_unicode` → TODO (izomorfní úklid, ne scope dnešní volby).

## Kód

- **`build_dataset.py`** — `DATASET_MAPS` +2 záznamy (Bedrichovka/Blatna, `split="train"`, pgw_width
  = plná šířka). Komentář k ASCII name + georef ověření.
- **`output/scale_check.py`** (ad-hoc, gitignored) — bbox raster ink vs maska nonbg + centroid +
  zmenšený overlay náhled, unicode-safe zápis (`imencode`+`write_bytes`). Reality-check 3 párů.

## Metriky / ověření

| Mapa | CRS | georef (bbox/centroid) | plochy |
|---|---|---|---|
| Bedřichovka | S-JTSK Křovák Local | ✓ posun <0,3 % (centroid Δ −124/−101 na ~10k px) | green 24,5 % yellow 14,6 % +black/gray/brown/blue |
| Blatná | S-JTSK Křovák EPSG | ✓ posun ±1–41 px (centroid Δ −32/+3) | green 19,6 % yellow 7,5 % blue 0,5 % |
| SampleMap | UTM z10 | ✓ (žlutá sedí; bbox nevypovídá — 8 ploch) | jen yellow 11,8 % |

**Dataset rebuild** (regrese Slovanka/Garching **bit-identická**):

| split | mapy | dlaždic | dominantní třídy |
|---|---|---|---|
| train | Slovanka + Bedrichovka + Blatna | 234 → **997** | green 58 % yellow 39 % + **gray 0,8 / brown 0,7 / black 0,9 %** |
| val | Slovanka (within) | 62 (beze změny) | yellow 67 % green 31 % |
| test | Garching (cross) | 22 (beze změny) | brown 33 % green 28 % gray 23 % yellow 16 % |

Integrita: 1081 dlaždic, **0 chybějících** image/mask souborů, ASCII jména (žádný mojibake).
Manifest formát beze změny → `train.py` kompatibilní konstrukcí. Dataset zazipován pro „mrkla".

**Dopad na class gap:** train dřív (Slovanka-only) gray/brown/black ≈ 0 → Garching test je
brown 33 % + gray 23 % nebylo jak trefit. Teď train obsahuje gray/brown/black (z Bedřichovky) —
přímo míří na S15/S16 blocker. Pozn.: gray v Bedřichovce (lesní = skála) ≠ Garching gray (sprint
budovy) sémanticky, ale stejná třída masky — jestli to pomůže, ukáže až trénink.

## Kudos / Censure

- **Kudos! (AI proces):** reality-check bbox+centroid+overlay od začátku (ne oko na overlayi) —
  dotažené S16 poučení; ověření „944 skip = bug?" proti zdroji (verify-against-source) místo domněnky.
- **Kudos! (uživatel):** přidání 2 nových domén (Křovák) jako reálný stress-test georefu — odhalilo
  pozitivní generalizaci, kterou by syntetická data neukázala.
- **Censure! (AI, sobě):** první spuštění `scale_check.py` spadlo 2× za sebou na drobnostech
  (chybějící `force_utf8_console`, pak `cv2.imwrite` mojibake) — měl jsem unicode/console hygienu
  (známou z S16 imread fixu) ošetřit v skriptu hned, ne reaktivně po pádu.

## Příště

1. **Plný trénink na „mrkla"** (GPU, 40 epoch) — teď s **bohatším datasetem** (3 mapy, gray/brown/black
   v trainu) + opraveným georefem = první poctivá cross-domain metrika. Re-eval přes `eval.py`.
2. **`imwrite_unicode`** v `cli_utils` (izomorfní s `imread_unicode`) — odstranit ASCII-name workaround
   + opravit `build_dataset:146`, probes. Drobné.
3. **Audit follow-up kód:** `parse_coords` kanonizace, `priority==index` assert, `compare_to_omap` rozdělení.
4. **Víc domén** — pořád chybí sprint/urbánní train mapa (Garching je jediná ISSprOM, jen v testu).
