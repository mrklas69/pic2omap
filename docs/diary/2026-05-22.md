# 2026-05-22 — Sezení 18

První sezení dne. `%BEGIN` audit cadence: nic překročeno (CODE 4/8, DOCS 2/10,
prune 2/12, CALIBRATE 2/15 — tři audity proběhly v S14/S16). **Stale Příště:** trojice
(plný trénink / víc map / audit follow-up) se opakuje **4× po sobě** (S14→S17), práh 5 —
příště překročí. Přístup na „mrkla" (GPU) je až za pár dní → plný trénink (4 sezení #1)
blokuje externí stroj. Fokus zvolen v `%BEGIN`: **audit follow-up kód** (dev-doable,
předběhne stale práh). Scope odsouhlasen: **#2 assert + #1 parse_coords (bez georefu)**,
#3 `compare_to_omap` split odložen na vlastní sezení.

## Diskuse

- **„3× duplicita" coord-parsingu je ve skutečnosti 2+1** (ověřeno proti zdroji před kódem).
  `omap_mask._parse_coords` (split, drží flag) a `compare_to_omap._object_centroid` (regex
  na jeden `<coords>` text) berou **jeden coords text** → sjednotí se čistě. `georef.
  _compute_coord_bbox` ale skenuje regexem přes **celý `<objects>` blob** (bbox-only, vědomě
  bez full-parse) — jiná operace, ne kopie. TODO popis („3× → 1 parse_coords") byl proto
  optimistický.
- **Po migraci prvních dvou zůstane georef jediným regex skenerem** → duplicita zmizí, aniž
  sáhnu do nejcitlivějšího modulu. Předělávat `_compute_coord_bbox` na ET-parse by přineslo
  marginální DRY za georef-regresní riziko (bbox-fit kalibrace forest Local CRS) → KISS, nechat.
- **`priority == index` je dvojí indexovací schéma vedle sebe.** `get_color(ref)` dělá
  `colors[ref]` (poziční), kde `ref` je **priority** hodnota; `build_category_map` zase
  klíčuje podle `priority`. Mlčky předpokládají, že splývají — když ne, `get_color` tiše vrátí
  špatnou barvu. Invariant ověřen ve 3 reálných (S14), ale kód ho nevaliduje.
- **Realistický scope (poučení ze S14 censure).** `compare_to_omap` split (846 ř., čistě
  strukturní, žádná změna chování) je největší a nejméně urgentní → vlastní sezení, nebundlovat.

## Rozhodnutí

- **`parse_coords(text) -> [(x,y,flag)]` v `omap_parser`** (single source of truth, split-based).
  `omap_mask._parse_coords` smazán → import sdílené; `compare_to_omap._object_centroid`
  přepsán na `parse_coords` (lokální `import re` odstraněn). `georef._compute_coord_bbox`
  **vědomě ponechán** (jediný zbylý regex skener, jiný vstup = blob).
- **`priority == index` = hard assert (fail-loud), volba uživatele.** V parse loopu barev
  `colors[i].priority != i` → `SystemExit` s jasnou hláškou. Fail-loud bije tiše-špatnou-barvu.
- **#3 `compare_to_omap` split odložen** — vlastní sezení.

## Kód

- **`omap_parser.py`** — nová `parse_coords(text)` (po `iter_map_objects`, sekce objects/coords);
  parse loop barev: `enumerate` + assert `priority == index` (`SystemExit` s názvem souboru).
- **`omap_mask.py`** — lokální `_parse_coords` smazán, import `parse_coords` z `omap_parser`;
  volání v `build_area_mask` přepsáno; komentář na single-source-of-truth.
- **`compare_to_omap.py`** — `_object_centroid` přes `parse_coords` (regex + `import re` pryč);
  import `parse_coords`.

## Metriky / ověření

Baseline-driven regrese (`output/regr_baseline.py`, gitignored — před↔po):

| Tok | Test | Výsledek |
|---|---|---|
| omap_mask (forest/Slovanka/Garching) | md5 masky (parse_coords + assert) | **bit-identické** |
| _object_centroid (24 777 GT objektů) | md5 centroidů | **identické** |
| assert — negativní test | vadné `priority` v temp `.omap` | **pálí** (`SystemExit`, ne no-op) |
| assert — 3 reálné soubory | invariant `priority==index` | prochází |
| `py_compile` všech root `.py` | — | OK |

`detect` 722 nespouštěn záměrně: parse_coords je mask/compare-only, assert je v `parse_omap`
(už proběhl přes mask runy) — změny **nejsou v detekčním toku**, artefakt regrese = masky + centroidy.

## Kudos / Censure

- **Kudos! (AI proces):** ověření „3× duplicita" proti zdroji před kódem odhalilo, že je to
  2+1 (georef = jiná operace) → čistá DRY bez sáhnutí do georefu, místo riskantního refaktoru
  dle optimistického TODO. Negativní test assertu (že opravdu pálí, ne no-op).
- **Kudos! (uživatel):** rozhodnutí `%END` po uzavřeném celku místo cpát další práci do sezení
  (mrkla netlačí) — čistá commit/diář hranice.
- **Censure! (AI, sobě):** TODO formulace „3× → 1 parse_coords" jsem do S18 přejal jako fakt;
  teprve čtení kódu ukázalo, že je nepřesná. Audit follow-up položky by měly nést i mini-caveat
  „ověřit shape duplicit", ne jen závěr.

## Příště

1. **Plný trénink na „mrkla"** (GPU, 40 epoch) — **blokuje přístup na stroj (~pár dní)**. Bohatší
   dataset (3 mapy, gray/brown/black) + opravený georef = první poctivá cross-domain metrika. Re-eval `eval.py`.
2. **#3 `compare_to_omap` split** — extrakce `review_export.py` (CSV/review). Zbytek audit follow-upu, vlastní sezení.
3. **`imwrite_unicode`** v `cli_utils` (izomorfní s `imread_unicode`) — odstranit ASCII-name workaround + probes.
4. **cv2 detekční track** (volitelně, dokud mrkla není) — point detektor v2 / brown-line precision.
5. **⚠ Stale Příště:** plný trénink po S18 = 5× po sobě → příští sezení DO (jakmile mrkla) / explicitně držet jako blokované-externě.
